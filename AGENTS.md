# AI 营销素材智能生成平台协作规则

## 0. 客观协作原则

- 不要默认认同用户或既有实现。行动前先检查错误前提、逻辑跳跃、信息缺失与未经证实的判断。
- 发现问题时直接指出，并说明证据、影响、反例或更合理方案；语气可以直接，但不能刻意尖刻。
- 明确区分已确认事实、合理推断、个人建议和暂时无法验证的信息。不得编造接口、数据、测试结果或业务结论。
- 用户提出的方案与项目事实冲突时，以代码、契约、已确认计划和本文件定义的固定流程为准，并说明冲突。
- 先理解现有代码、文档与数据流，再设计和实现；禁止只看单个页面就重造第二套状态、接口或组件。

## 1. 项目定位与固定工作流

本项目是内部使用的 AI 营销视频生产平台，采用前后端分离架构。一个项目可运行多条工作流，但各工作流的运行状态必须按 `projectId + workflowType + workflowRunId` 隔离。

五条固定工作流如下，节点不可随意增删、合并或改义：

1. 效果类：`资料导入 → AI 信息提炼 → Prompt 生成 → 视频渲染 → 模板混剪 → 成片输出`
2. 定制类：`资料包导入 → 标准化提炼 → 分镜脚本 → 镜头 Prompt → 视觉资产 → 逐镜渲染 → 素材交付`
3. 裂变类·爆款复刻：`上传素材 → 深度拆解 → 元素映射 → 成片生成`
4. 裂变类·数字人口播：`选择形象 → 口播文案 → 唇形驱动 → 背景配置 → 渲染输出`
5. 裂变类·局部元素替换：`选择成片 → 替换粒度 → 解析与替换 → 生成提示词 → 渲染输出`

效果类的关键语义：Prompt 节点生成的是用于渲染碎片化视频素材的片段级 Prompt，不是完整成片脚本；固定一条 Prompt 对应一个素材片段。渲染节点只生成素材片段池；混剪节点按模板槽位、标签与去重规则将多个片段组成独立时间轴工程；只有成片输出节点才渲染最终成片。

效果类 AI 信息提炼的长期口径：当前产品素材制作信息卡使用 schema v3。`targetAudiences[]` 是可编辑、可映射的权威受众事实，`targetAudience` 只由服务端派生用于历史兼容，前端和下游不得把摘要重新当成第二份事实源。制作规则中的分辨率只能使用火山接口支持的 `480p / 720p / 1080p`，资料导入、信息卡、Prompt 约束和后续渲染参数必须保持同一值；历史合法大写值只做读取归一化，不允许继续保存任意字符串。

效果类 Prompt 的长期口径：新批次只允许用户设置总数量和统一默认时长；六类片段是生成后的推荐用途与兼容用途，不得重新作为生成配额、六条独立生成分支或人工固定标签。每条 Prompt 的正文与“叙事结构、场景、人物、产品关联点、镜头语言、情绪基调”六维信息必须围绕同一个创意同步生成；`产品关联点` 取代旧版 `卖点侧重`。批次共用提示词必须维持一段权威文本，生成时用于约束，视频请求编译时只追加一次，不得复制进每条正文。当前公开工作流固定使用 `CURRENT` 八阶段拓扑：输入快照、提炼信息映射、事实视觉使用策略编译、共用提示词编译、连贯六维创意生成、创意评估与用途分类、精确数量择优与补充、结果保存。旧版图标识只允许作为数据库审计兼容常量，不得重新出现在页面、当前节点详情或新 Worker 执行路径。

Prompt 选择的首要目标是严格达到用户数量。结构破损、未确认事实和实质完全重复仍可硬拒绝；一般语义相似只参与向量 MMR 排序和软告警，不得造成数量阻断。当前向量择优只嵌入清洗后的 Prompt 正文，按 `70%` 独立质量和 `30%` 相对新颖度逐条选择，并把人工保留项或单条重生成时的其他条目作为固定参照。人工新增或修改后必须进入异步 `ITEM_EVALUATE`，评估完成前不得提交 WorkingArtifact。

具体节点职责和上下游关系必须阅读对应工作流指南：

- [效果类](docs/workflows/effect/agent-guide.md)
- [定制类](docs/workflows/customized/agent-guide.md)
- [爆款复刻](docs/workflows/fission/clone/agent-guide.md)
- [数字人口播](docs/workflows/fission/avatar/agent-guide.md)
- [局部元素替换](docs/workflows/fission/local-replace/agent-guide.md)

## 2. 四层指令管理

| 层级         | 存放位置                                | 只放什么                                       | 不应放什么                 |
| ------------ | --------------------------------------- | ---------------------------------------------- | -------------------------- |
| 长期项目规则 | 根目录与子目录 `AGENTS.md`              | 长期稳定的边界、业务真相、目录规则             | 一次性实施步骤、角色提示词 |
| 可复用流程   | `.agents/skills/*/SKILL.md`             | 可重复执行的节点开发、原型还原流程             | 某次任务结论、庞大背景说明 |
| 专职角色     | `.codex/agents/*.toml`                  | 可并行委派的只读分析、契约审查、浏览器验收角色 | 主代理必须亲自完成的写操作 |
| 兜底检查     | `.codex/hooks.json` 与 `.codex/hooks/*` | 机械、确定、快速的停止前检查                   | 依赖主观判断的业务评审     |

Hook 不是安全边界，也不能代替测试和代码评审。合并门禁最终应由 CI 执行；本仓库的 Hook 只做本地快速兜底。

## 3. 必读路由

修改代码前，按任务范围额外阅读：

- `apps/web/**`：`apps/web/AGENTS.md`
- `apps/api/**`：`apps/api/AGENTS.md`
- `packages/contracts/**`：`packages/contracts/AGENTS.md`
- `workers/**`：`workers/AGENTS.md`
- `workers/effect-extraction/**`：再读 `workers/effect-extraction/AGENTS.md`
- 本地部署、Compose 或开发环境：`docs/development/local-development.md`
- 任意工作流节点：对应 `docs/workflows/` 业务目录下的 `agent-guide.md`
- 基于原型开发 UI：`docs/development/agent-guides/prototypes.md`，并使用 `$reproduce-prototype-ui`
- 开发或重构工作流节点：使用 `$develop-workflow-node`
- 已确认的实施计划：只读取与当前任务直接相关的 `docs/` 计划，不把旧计划当成永恒业务事实。

子目录 `AGENTS.md` 只补充所属目录规则；若与根规则冲突，以更具体且不改变固定业务流程的规则为准。

## 4. 技术栈与目录边界

- Web：Vue 3、TypeScript、Vite、Pinia、Vue Router、Axios。
- API：NestJS、TypeScript、Prisma、PostgreSQL、RabbitMQ Outbox、Redis、S3/MinIO。
- AI Worker：Python、FastAPI、LangGraph、Pydantic、httpx、NumPy（Prompt 向量 MMR）。
- 外部视频生成优先通过 Seedance/Ark 等适配器接入，不允许页面或业务服务直接绑定厂商协议。

主要目录：

- `apps/web`：正式前端，只处理展示、交互和前端状态。
- `apps/api`：业务 API、权限、事务、任务编排和持久化。
- `packages/contracts`：前后端与 Worker 共用契约，是跨进程字段的唯一来源。
- `workers/<workflow>`：按工作流隔离的异步 Worker。
- `docs`：按 `architecture / project-assets / development / workflows` 隔离。所有新实施计划必须进入所属业务的 `plans/`，不得堆回 `docs/` 根目录或只留在对话中。
- `references/prototypes`：冻结原型，仅作视觉与交互基线，不作为运行时代码。

禁止：

- Web 直接访问数据库、对象存储或模型供应商。
- API 与 Worker 复制定义同一个跨进程 DTO。
- 跨工作流直接导入另一工作流的内部模块。
- 为解决局部需求建立第二套项目、资产、任务或版本状态。

## 5. 工作状态、工作副本与资产

- `WorkflowNodeState`：每个工作流节点一份自动保存的编辑状态，用于恢复界面，不代表已确认产物。
- `WorkingArtifact`：某类业务产物唯一的最新工作副本。粒度由业务对象决定，不等于“每个节点只有一个”。
- 用户在节点内反复编辑可以只更新草稿；在“完成校验/确认结果”等明确提交点，才提交新的工作副本 revision。
- revision 表示已确认业务内容发生变化，不表示每一次输入操作。提交后内容指纹与上一个已确认版本相同，则不得增加 revision。
- 下游必须记录所消费的上游 `artifactId + revision`（必要时加内容指纹），而不是绑定“节点当前 revision”这一模糊概念。
- 上游确认产生新 revision 后，下游标记待更新；尚未生成的下游不需要伪造工作副本。
- `ProjectAsset` 是项目内正式归档资产；`GlobalAsset` 是经过明确发布、可跨项目复用的资产。工作副本不能自动等同于正式资产。
- 项目临时退出时保存节点草稿和工作区状态；完成工作流或用户明确归档时，再批量沉淀项目资产。

## 6. 外部任务与安全

- 长耗时模型调用采用异步任务：API 创建 Run/Task，Worker 消费并回写状态，前端轮询或订阅状态。
- 创建任务、回调和重试必须幂等；任务状态至少覆盖排队、运行、成功、失败、取消。
- 数据库事务内只写业务状态与 Outbox，外部调用在事务外执行。
- 密钥只从环境变量或密钥服务读取，日志和错误中不得输出 Token、Cookie、签名 URL 或用户原始敏感资料。
- Mock 必须显式启用；缺失真实配置时默认失败，不能静默退回 Mock 并伪装成功。

## 7. 计划、测试与 Git

- 复杂改造先在所属 `docs/**/plans/` 建立可验收计划，再实施；计划必须写清范围、非目标、数据接口、迁移和测试。
- 只修改当前任务需要的文件，保留用户已有改动。禁止使用破坏性 Git 命令覆盖脏工作区。
- 验证应与风险匹配：至少执行相关单元测试、类型/语法检查和 `git diff --check`；涉及跨层契约时验证 Web、API、Contracts、Worker。
- UI 改动必须在浏览器回归关键宽度、加载/空/错误/完成状态和主要交互。
- 不得声称未实际执行的测试已通过。
- 提交信息使用中文，描述业务结果；分类时使用“功能：”“修正：”“文档：”“重构：”“工程：”“测试：”等中文前缀，不使用 `feat:`、`fix:` 等英文前缀。除非用户明确要求，不自动提交、推送或创建 PR。

## 8. 开发前自检

开始实现前，必须能回答：

1. 当前属于哪条工作流、哪个节点？
2. 直接上游输入是什么，消费了哪个工作副本 revision？
3. 当前节点输出的工作副本和提交边界是什么？
4. 哪些下游会因本次确认变更而待更新？
5. 是否存在正式原型、共用契约或既有实现必须复用？
6. 需要读哪个子目录 `AGENTS.md`、工作流指南和 Skill？

无法回答时先查代码与文档，不要凭想象实现。
