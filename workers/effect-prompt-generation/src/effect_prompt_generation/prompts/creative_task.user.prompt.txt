逐条事实任务简报：
{task_briefs_json}

本批次统一共用约束（仅遵守，不复述）：
{shared_prompt_content_json}

需要避开的已有语义签名：{avoid_semantic_json}
需要避开的已有视觉签名：{avoid_visual_json}
上一轮淘汰原因：{rejection_reasons_json}
单条重新生成上下文：{regeneration_context_json}

为每个任务生成一条独立、完整、连贯且与当前商品有关的素材创意。focusFact 是本条必须完整使用的业务重点，不得用 productSnapshot 中的商品名称、规格或外观替代它。按 focusFact.realizationField 让重点事实真正决定人物、场景或产品关联点，并使正文画面与之相符；如果该事实不能由画面直接证明，就把它作为安全的商业语境，不要编造证明画面。

allowedFacts 只供自然搭配，productSnapshot 只负责真实性边界。creativeDirection 提供批次差异方向，priorityDimensions 是最值得变化的两个维度，avoidFamilies 是应避开的拥挤套路；不要机械复述这些字段，也不要按六类用途分组。

每条必须返回 focusFactId 和 focusFactEvidence。focusFactEvidence.evidenceText 必须逐字存在于它声明的 evidenceSource 字段中。已有语义和视觉签名只是软避重参考，差异不能以牺牲事实、商品关联或画面逻辑为代价。
