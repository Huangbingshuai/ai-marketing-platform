需要新增的创意方向数量：{requested_direction_count}
每个补充方向的执行路线数量：{execution_route_count}

已确认事实：
{facts_json}

事实视觉使用策略：
{fact_visual_strategy_json}

批次共用提示词（只约束，不复述）：
{shared_prompt_json}

产品专属创意版图：
{creative_landscape_json}

可直接复制的合法 ID 组合表：
{allowed_combinations_json}

现有方向摘要（保留每条路线的视觉事件、场景关系、产品动作及结束状态，用于全批避重）：
{existing_directions_json}

当前拥挤场景语义族：
{crowded_scene_families_json}

当前拥挤产品动作语义族：
{crowded_action_families_json}

全批复核后的修订上下文（首次为空）：
{revision_context_json}

directions 必须恰好输出 {requested_direction_count} 个真正新增的方向。每个方向从合法 ID 组合表选择 territoryId 及同一行的 factApplications.factId；primaryActionId 选择该行旧动作或本方向 proposedAction 新提议的 ID，新提议不得覆盖旧 ID。不要凭语义猜测已有 ID 的归属。优先使用现有批次较少出现、但与产品和事实自然相容的事件与画面关系。每个方向的 executionRoutes 必须恰好输出 {execution_route_count} 条。
若上下文包含 requiredDirectionIds，只输出这些 ID 对应的待生成或待修复方向，逐个完整返回且不改 ID；retainedDirectionIds 中的方向已通过来源与结构校验，不得重写。数量减少表示局部修复，不表示降低创意要求。根据 directionErrors 修复该方向的合法组合，根据全批复核意见修订画面关系；不能用替换事实 ID 来冒充已修复的创意。
