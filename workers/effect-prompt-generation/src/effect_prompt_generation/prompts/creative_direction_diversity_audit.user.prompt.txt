产品专属创意版图摘要：
{creative_landscape_json}

本批全部创意方向摘要：
{creative_directions_json}

本次新提出的补充方向 ID（常规全批复核时为空）：
{proposed_direction_ids_json}

请从“渲染后是否仍会得到近似画面”的角度进行全批比较。groups 只记录实质可互换的方向组，不把普通同品类、同风格或同一产品视为重复。

同时输出 canonicalProfiles，并且必须恰好覆盖上述每一个 directionId。先在整批内部建立统一的上位叙事族、场景族、人物关系族、产品主动作族、镜头族和情绪族，再为每个方向填写；同义表达必须使用同一个标签，真实画面关系不同才使用不同标签。
