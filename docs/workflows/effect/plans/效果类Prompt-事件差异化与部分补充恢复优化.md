# 效果类 Prompt：事件差异化与部分补充恢复优化

状态：实现、自动回归及一次浏览器真实验收完成；重复度改善，综合质量仍未达预期。2026-09-10。

## 依据与目标

正式紫苏梅子酱项目上一轮 50 条 × 15 秒：语义重复度 52%，初始目标 70 条实际保留 55 条，事实补充后候选池 65 条；多样性补充的少数方向持续结构错误，导致已保留的有效方向未进入 AI 复核。独立事实和不同六维标签不等于不同画面事件。

目标：从规划和生成输入减少近似事件，恢复有效补充利用率；不调高重复阈值，不用降低质量标准换数量，不做单商品规则。

## 范围与非目标

- 仅效果类 Prompt Worker 内部及模板、测试；不新增节点，不修改公开 DTO、HTTP、Prisma、模型路由、候选总量或评分/MMR 权重。
- 沿用 `marketing-insight:{productId}` 的已提交 artifact/revision 快照及统一卖点；当前正式测试基线消费提炼 revision 1。
- 自动生成仅更新领域草稿与 WorkflowNodeState；用户完成校验才提交 `prompt-batch:{productId}`，之后视频渲染消费该 revision。此次改造不确认、不覆盖工作副本、不运行视频渲染。
- Worker 仅处理来源、ID、结构、数量、重试和 AI 结论；事件相似、事实关系和物理合理性由 AI 判断，不引入字符匹配或商品关键词。

## 实施

1. 规划模板以可见事件和商品价值为差异单位；区分独立素材与同事件替代机位，事实数量不能冒充创意容量。不增加输出字段或硬门禁。
2. 候选模型始终接收所属方向的兄弟路线精简摘要，即使分片或失败拆分只剩一条，也能比较本条与其他路线。只给该方向的路线，不给整批规则表；每条保持一个简单事件，不通过增加操作复杂度求不同。
3. 全批 AI 复核及分类强调上位动作关系，不按物品、修饰词或细分角度无限细分标签；保留同动作但真实价值不同的合理素材。
4. 多样性补充保留稳定 ID 的结构有效方向。原有结构恢复预算耗尽时停止补缺，对保留部分执行原有关系/全批重叠 AI 复核；只有复核通过才投入生成。无有效方向或复核失败仍返回空，不伪造通过、不新增无限重试。
5. 延续上一轮的失败分片顺序恢复修复，避免首个子分片失败后其余付费调用游离运行；正常生成并发不变。

## 数据与兼容

扩充模型输入简报中的兄弟路线摘要、全批复核路线关系及补充样例的实际事件，均来自既有 executionRoutes / shotPlan；不改模型输出或历史分片结构。旧任务无路线时摘要为空，无 shotPlan 时保留主线和六维。模板哈希沿用现有内容哈希机制。补充方向实际数量按通过复核的数量记录；可选多样性候选数不超过通过复核的路线总量，避免少数路线循环复用。最终目标数量、原有数量补充机制不变，不跨 Run 复用。

## 验证

- 模板及 Provider 输入测试：拆分单条仍看到兄弟事件，不夹带其他方向、全批事实或候选正文。
- 补充恢复：部分成功、全失败、AI 不通过、AI 异常、取消、稳定 ID 与未点名方向保留、调用预算、实际数量。
- Worker 全量非付费 pytest、mypy、Ruff、git diff --check。
- 实现后不把自动测试通过等同于重复度改善。后续真实测试须从正式浏览器发起，使用正确项目、同数量/时长/风格比较；不自动提交 Git。

## 实施与验证记录

- 已修改空间、方向、候选、全批复核及补充模板，按事件而非装饰差异组织创意；未增加输出字段、模型调用阶段或评分门槛。
- Provider 投影原有兄弟路线与真实事件，Worker 不生成、解释或归类任何事件文本。
- 部分补充经 AI 复核后可投入生成；测试覆盖 4 个方向中持续失败 1 个时保留 3 个，实际生成 6 条不同路线，而不是重复填满 8 条。全失败、复核拒绝、复核结构错误、取消和整体响应失败均有回归。
- 已通过全批 AI 复核的新方向不再被分配器按拥挤上位族二次过滤：同族可以有不同事件，避免过滤后反复复用仅剩路线。回归验证共享场景族的新方向仍使用全部 6 条不同路线。拥挤信息仍传给 AI 做补充设计，最终向量择优不变。
- 已保留上一轮顺序拆分恢复修复及其他已有改动；未触碰并行任务的 Web、API、提炼节点和素材文件。
- `uv run pytest -m "not ark_integration"`：392 passed，1 deselected（付费集成测试未执行）。
- `uv run mypy src`：25 个源文件通过；`uv run ruff check src tests`：通过；`git diff --check`：通过，只有现有 LF/CRLF 提醒。
- 此轮未部署或重启运行中的 Worker，未发起新付费批次、未提交工作副本、未提交或推送 Git。52% 仍是上一轮真实基线，不宣称本轮已改善到某个百分比；下一轮须从正式浏览器发起同配置测试后比较。

## 提交后真实验收（后续执行记录）

用户随后授权“提交 Git 后自行付费测试”。已提交 `7c2def2`（优化：增强素材事件差异并保留有效补充方向），仅纳入 Prompt Worker、测试和本计划，没有提交其他并行修改或推送。空闲时更新并重启 Prompt Worker，核对运行中模板哈希与当前源码一致，然后从正式浏览器发起一次真实批次。

### 输入与结果

- 正式“紫苏梅子酱”项目，不是旧浏览器联调项目；已提交提炼 revision 1，共 40 条卖点和 5 项产品基础。
- 50 条 × 15 秒，AI 根据场景选择风格，京东。没有修改事实、重复阈值、MMR 权重或模型路由。
- 北京时间 2026-09-10 17:47:12 至 18:23:07，35 分 55 秒；Run 第 1 次执行完成，没有整批重启。
- 50 条均 VERIFIED，批次 NEEDS_REVIEW，仅保存领域草稿，commitStatus 为 UNVALIDATED、工作副本 revision 为空。没有点击完成校验，也没有调用视频渲染。

| 同配置对比 | 上一轮 | 本轮 |
| --- | ---: | ---: |
| 最终数量 | 50 | 50 |
| 算法语义重复度 | 52% | 30% |
| 冗余条目 / 近似组 | 26 / 16 | 15 / 11 |
| 高相似组合 | 48 对 | 29 对 |
| 完全相同正文 | 0 | 0 |
| AI 五项加权平均分 | 92.215 | 92.829 |
| 初始实际候选 / 计划 | 55 / 70 | 57 / 70 |
| 补充后候选池 | 65 | 67 |
| 评分硬淘汰 | 0 | 0 |
| 最终实现卖点 | 28 | 26 |
| 背景保留卖点 | 9 | 10 |
| 应呈现但缺失卖点 | 3 | 4 |
| 总耗时 | 32 分 34 秒 | 35 分 55 秒 |

重复度下降 22 个百分点，约相对下降 42.3%，但仍高于 15% 参考；一次随机批次不足以证明稳定改善。AI 分数不是人工质量验收结论。两次 AI 视觉策略的背景划分不同，因此不能只比较“实现卖点数”便认定输入丢失；两轮均完整收到 40 条卖点。本轮最终为 26 项实现、10 项背景、4 项缺失，合计 40。

### 源头与择优观察

- 8 个空间、20 个方向，规划引用 39 项卖点。初始候选经评估全部 57 条保留，首次择优重复度 40%；事实覆盖补充 10 条后重新择优，最终为 30%。
- 最终缺失：红烧鱼用法、茄子煲收汁、披萨底酱、鸡翅腌烤效果。择优节点与结果保存的缺失数均为 4，口径一致。
- 本轮纯质量择优冗余 21 条，MMR 后为 15 条；高相似组合 39 → 29，对应加权平均分 93.037 → 92.829。排序有效，但不能生成候选池没有的事件。
- 多样性方向补充调用 4 次，完成 2 次全批 AI 复核，最终没有被采纳的新方向或候选；因此不得把本轮下降归因于“多样性补充成功”。部分恢复机制已进入真实链路，但本次未证明部分方向投入生成的最终效果。
- P045/P047 相似度 0.9103，P037/P045 为 0.9078，P037/P047 为 0.9039：都是炸鸡蘸酱后展示，只改变整蘸、半蘸、放盘关系。同事实分配不同、场景族不同，仍可产生同一观看事件。
- 最终六维共 299 个不同文本值（人物 49，其余各 50），但算法仍识别 15 个冗余项。不能用六维文字唯一率代替画面事件差异。

### 逐条人工审查结论

已阅读 67 条候选，并逐条对应最终 50 条编号；对评分后发生修改的正文单独复核。以下均为最终编号，不是生成槽位编号。

- P008、P017、P042：同时出现固定机位与跟移/平移等描述，拍摄方式仍有冲突或歧义；P010 的 AI 修复只处理了前一段，后一段仍有“固定机位随……上移小幅跟拍”。
- P025、P042：要求少量勺中酱体长时间维持连续酱柱，甚至携带果肉悬于流柱，物性表现仍过度理想化。上游确认缓慢回落，不等于确认持续 15 秒稳定不断。
- P038：两瓶同步反向旋转半圈，却要求正面与侧面互换，角位移与结束朝向不一致；虽然有转盘驱动字段，依然存在空间逻辑问题。
- P022：秋葵横向架置，却让重力驱动酱体沿棱线向右长距离流动，缺少明确坡度与支撑关系。
- P011：筷子夹着薄牛肉片一端，随后要求水平展开并保持展示，缺少足够支撑说明；P046 同一只手兼持酱罐与勺，仍有多物体操作风险。
- P014、P043：嫩豆腐上的精细放射纹、规整酱环和指定扩散形态仍偏难控；P033 数秒盖锅后要求酱汁明显浓缩，过程表达仍不充分。
- P002 的手扶锅沿、P028 颠锅等仍值得优化；不得把“单个动作有驱动说明”当作物理合理性已经解决。
- 本轮未发现明确人物口播/逐字台词安排，但静音素材价值大量收敛到上色、包裹、铺展，营销事件的多样性仍不足。

以上是 Prompt 人工风险判断，没有生成视频，不能写成已观察到实际视频故障。人工审查仍发现质量问题，故不判定综合质量达标。

### 稳定性与浏览器

- 生成阶段 4 个持续结构失败分片共损失 13 条初始候选。另有结束画面字段超长、执行修订理由超长、评估 JSON 解析失败、补充 JSON 解析失败，以及前期 3 次网络连接中断。没有把这些失败归咎于事实语义，当前日志也不足以细分所有候选丢失原因。
- 日志记录 156 次成功的结构化 Ark 响应、9 次失败调用记录；成功响应输入约 146.24 万 token、输出约 25.31 万 token，另有 67 次向量请求。这不是计费账单：不包含全部失败响应消耗，不估算未经核对的实际金额。
- 前期规划/复核/修订元数据记录 55 次 AI 调用，正文生成前已消耗约 14 分 18 秒。恢复与补充增加总耗时，本轮不能称为性能优化成功。
- 浏览器刷新后恢复正确项目、50/50、30.0%、AI 自动风格、5 条/页共 10 页；翻至第 2 页加载正常，重新生成与完成校验按钮恢复可用，没有提交。
- 正常导航与生成页面控制台无报错。验收末尾只读诊断误加 `/workspace` 路径产生一次 404，随后使用页面实际根路径读取成功；这是诊断探针错误，不是业务请求失败，不隐瞒为全程零错误。
- 完整结果与阶段快照仅保存在 Git 忽略的 `.tmp/plum-unified-paid-20260910/`，不将完整 Prompt、内部 ID、原始模型响应或敏感配置写入仓库。

### 后续建议（未自动实施）

下一步优先拆清三件事：AI 规划的同方向路线容量仍偏乐观；AI 最小修订未完整解决镜头及物体状态矛盾；结构失败恢复缺少明确的分原因诊断。优化应继续围绕实际事件、合理初末态与有限差异，而非增加 Worker 语义判定、商品专用规则或放宽重复阈值。本轮不追加第二次付费批次。
