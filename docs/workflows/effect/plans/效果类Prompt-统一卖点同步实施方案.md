# 效果类 Prompt：统一卖点同步实施方案

状态：实现与自动回归已完成；未进行付费生成质量验收

## 范围与边界

同步已确认的“产品基础＋sellingPoints”提炼结构。直接上游仍为 `marketing-insight:{productId}` 的 artifactId、revision 和内容哈希；生成、人工编辑及评估只写领域草稿和 WorkflowNodeState，明确完成校验才提交 `prompt-batch:{productId}`，使已有视频渲染下游待更新。读取旧提炼工作副本由 API 唯一归一化入口处理，不改库、不增加 revision。

保留八阶段、模型选型、无口播素材定位、15 秒上限、四类用途与向量择优。不增加旧字段分类模型，不让 Worker 解释事实、证据、场景或动作语义。不覆盖提炼节点正在进行的改动。

## 实施

1. 当前事实使用中性 SELLING_POINT；历史字段继续可读取。产品基础和全部合法卖点完整映射，取消 500 字截断和不确定词正则排除。
2. 对齐共享契约、API、Worker 的 1000 字卖点和 105 项事实容量；策略按数量/文本预算分片，保留完整目录及跨事实引用，验证汇总完整性；重试不丢成功分片。
3. 复用 AI 视觉策略识别表达边界。上下文事实保留给规划，和画面覆盖目标分开；不因覆盖要求生成口播、字幕或虚构证明。
4. 模板按统一事实自然组合，不恢复受众/痛点/场景词池。事实多而素材少时不做机械容量硬阻断，遗漏作为覆盖提醒；不由 Worker 随机补挂事实。
5. AI 判断实际实现，Worker 只校验来源、ID、枚举、数量、结构和哈希。完整保存合法绑定，不以展示样例数量截断业务结果。最终覆盖以保存结果为准。
6. API 安全详情和 Web 标签同步为产品基础与卖点；区分接收、规划、最终体现及上下文保留，不伪造历史数据。

## 迁移与验收

无新数据库表、HTTP 路径、编号版本或业务分支。新 Run 使用当前归一化输入，旧结果的绑定与来源保留。仅本次执行输入/模板哈希控制派生检查点有效性，不更改业务 revision。

测试覆盖 1/100 条卖点、1000 字边界、相同前 500 字但不同全文、无关键词删除、策略分片完整性与重试、上下文保留、少素材多事实、超过五条有效绑定、历史读取、统一标签与覆盖统计。执行相关 Worker pytest/Ruff/mypy、Contracts/API/Web 测试及类型检查、构建、浏览器只读展示验证、git diff --check。本次不自动提交或推送，不自动触发付费生成或视频渲染。

## 实施与验收记录（2026-09-10）

- 共享事实枚举新增当前 SELLING_POINT，旧枚举只保留兼容；TypeScript/JSON Schema/Pydantic/API 支持单事实 1000 字与 105 项策略聚合。
- 视觉策略按最多 24 项或 12,000 字目标分片，完整事实目录不截断；通过现有阶段检查点恢复成功分片，并绑定输入与模板哈希。100 项卖点测试中断后只重做剩余 4 个分片，模板变化时全部重新执行。
- 规划保留全部事实；AI 判定的背景类事实转入上下文，不承担视觉覆盖目标。去除方向最低密度及缺失覆盖的结构硬阻断，保留事实来源、稳定 ID、空间/动作引用等确定性校验。
- 最终结果不再截断为前 5 个绑定。评估草稿与归一化结果均允许最多 12 项事实证据；测试验证 8 项有效绑定全部保存且参与最终覆盖统计。
- 自动验证稀疏卖点、100 项卖点仅生成 5 条素材、仅有背景卖点均可完成规划。五个品类的映射测试不依赖商品关键词；这些是 Mock/确定性验收，不是实际创意质量评分。

已执行并通过：

| 验证 | 结果 |
| --- | --- |
| Prompt Worker pytest，排除 ark_integration | 371 passed，1 deselected |
| Contracts | 26 passed |
| API Prompt 相关测试 | 104 passed |
| Web Prompt 相关测试 | 46 passed |
| Worker mypy | 25 个源文件通过 |
| Worker Ruff src/tests、本次 TS/Vue 专项 ESLint | 通过 |
| 工作区类型检查、Contracts/API/Web 生产构建 | 通过 |
| git diff --check | 通过 |

浏览器在正式本地页面只读检查：紫苏梅子酱历史批次可打开，详情输入显示产品基础及统一卖点（8 项预览、其余 20 项提示）；原阶段输出仍使用真实历史计数，不伪造新批次统计。390px 窄屏弹窗无横向溢出，ESC 关闭并将焦点恢复到“查看工作流”；刷新后重新进入 Prompt 可读取历史批次。首次会话有 favicon 404，刷新后控制台无错误。未点击重新生成、完成校验或视频渲染。

边界：本轮未证明付费模型质量、重复度、耗时改善；100 条长卖点的真实模型输入成本仍需独立付费验收。未提交或推送 Git，未覆盖用户已有工作副本。
