# Prompt 视觉事件修复：付费续测报告

日期：2026-09-08。授权上限 20 次，目前使用 1 次。仅效果类 Prompt，不渲染视频、不提交工作副本。

## 同源同设置对照

| 指标 | 对照批次 | 续测第 1 轮 |
| --- | ---: | ---: |
| 商品 / 设置 | 紫苏梅子酱，100 × 15 秒 | 相同 |
| 候选 / 最终数量 | 140 / 100 | 140 / 100 |
| 最终语义重复度 | 29% | 20% |
| 完全重复淘汰 / 硬问题淘汰 | 0 / 0 | 0 / 0 |
| 必用事实覆盖 | 26/26 | 26/26 |
| AI 加权平均质量 | 92.52 | 92.39 |
| AI 可执行性均分 | 91.36 | 90.13 |
| 动作过密提醒条数 | 6 | 15 |
| 总耗时 | 30 分 49.5 秒 | 34 分 55 秒 |

重复度下降 9 个百分点，但尚未低于 15%；速度和动作密度未改善，不能判定整体质量优化完成。评分来自现有 AI 评估，不是人工独立量表；没有生成视频，不能报告真实视频效果。

## 真实链路结果

- 方向规划拆小后，没有再出现输出截断；首次任务尝试即完成。157 次 Ark 请求中 151 次成功解析、6 次结构异常，通过既有局部恢复继续，没有整批重跑。
- 返回用量合计输入 1,439,144 Token、输出 341,685 Token，不含向量接口。不以 Token 数冒充实际账单金额。
- 多样性补充方向未被采用，最终仍为初始 140 个候选中择优 100 个；另 40 个是未入选的有效候选，不是 40 个硬淘汰。
- 浏览器 100/100、每条 15 秒、20 页 × 5 条、翻页、刷新、批量导出均验证；明确显示“尚未提交工作副本”。对照的上游信息、批次设置和 WorkingArtifact 未变。

## 人工诊断与修复

| 问题 | 证据 / 影响 | 当前处理 |
| --- | --- | --- |
| 有诊断却漏修 | 5 个候选有 executionFindings，但摘要 warnings 没有重复同码，触发器将其跳过 | 修正为使用合法 AI 定位诊断；新增自动回归 |
| 进度显示矛盾 | 阶段启动时的 0 与实时分片数混用 | API 同一安全投影统一计数 |
| 补充阶段不可感知 | AI 正在规划补充，但 Run 仍显示评估完成 | 调用前上报择优节点 RUNNING |
| 模型相对运动漏判 | 候选池存在“固定又平移”“同速环绕却展示各面”；这两个示例最终未入选 | 澄清 AI 模板中的相对运动与因果关系，Worker 不读文字作语义判断 |
| 动作仍可能过密 | 最终 P084 在最后 5 秒继续多次分装；P045 同段串联多种取用 | 保留为质量风险，不用数量门禁掩盖；漏修触发修正后需要真实复核 |
| 局部修订有效但有限 | 最终 P095 采用 AI 局部修订并复核，镜头与动作措辞改善；不意味着全批无冲突 | 下一轮继续人工复核入选正文，不仅看状态和分数 |

审查映射按最终正文与创意主线核对；候选序号不等于最终 Prompt 编号。完整正文与运行记录仅存放在 Git 忽略的本地验收目录，没有写入本报告。

## 验证与暂停点

Worker 299 项自动测试、mypy（23 文件）、Ruff；API Prompt 97 项测试、类型检查及生产构建通过。`git diff --check` 通过。新镜像构建完成。

测试 API/Worker 的重启命令被执行策略拦截，未绕过，未启动第 2 轮收费。上述新修正还需由允许的服务管理方式部署后，再通过浏览器发起同配置付费验证。当前结果不能算作这些新修正的验证结果。未自动提交或推送 Git。
