# 效果类 Prompt 分片流水与画面修正提速

状态：代码与自动回归完成，单轮真实付费后台对比完成；运行中浏览器联调通过，终态刷新验收受其他未提交前端改动阻挡。提速和重复度改善，人工画面质量尚未全面达标。基线：2026-09-14 广式腊肠 100 条 × 15 秒，36.51 分钟，最终语义重复度 14%，画面可执行性 89.32。

## 范围与边界

效果类 Prompt 节点消费 `marketing-insight:{productId}` 的已确认 revision；自动保存领域草稿与 WorkflowNodeState，只有用户完成校验才提交 `prompt-batch:{productId}` 并使渲染待更新。保持八阶段、HTTP、数据库、模型与数量优先选择规则。

## 实施

1. 默认 AI 并发 2 → 4，共享限流器；遇到真实 429 时降到 2，保留供应商退避机制。
2. 批量任务按分片执行生成、独立 AI 画面修正、评分，前一个分片评分与后一个分片生成重叠。评分前仅需本分片完成；向量与最终选择仍等待整轮。
3. 评分保持完整事实上下文与输入预算，输出预算由 6144 → 8192，使短候选有容量合成四条一组；长候选仍按现有机械 token 预算拆分。不得截短正文或事实来加速。
4. 独立修正调用逐条返回原样保留或完整修订。AI 负责画面、时间、工具、材料和主线/六维同步；Worker 仅检查身份、数量、结构并编译正文。新批次评分后不再执行旧修正-审片-复评环；评分建议有风险时仍可独立审片并记提醒。
5. 生成但未完成修正的分片保持未完成草稿。重试复用当前 Run 的原稿，成功分片不重新生成；分片取消或异常必须等待兄弟任务退出，禁止后台继续回写。
6. 单条人工编辑与历史结果保留现有路径。失败评分分片恢复原索引和分组；新增评分分片使用已持久化索引之后的新索引，成功评价不重复调用。

## 验收

Mock 覆盖并发上限/429降速、生成与评分重叠、完成顺序不影响数量与最终顺序、修正缺项/错ID失败、原样保留不改正文、修正失败恢复、补充也修正、人工编辑不改写。执行 Worker pytest、mypy、修改范围 Ruff 与 diff 检查。真实对比沿用同商品 100×15 的设置，记录耗时、调用数、重复率与人工画面问题；20～25分钟仅为目标，不预先声称达到。

## 迁移

不批量改写历史数据、不跨 Run 缓存。已成功且未评分的历史生成分片恢复后重新完成独立修正；已有成功评分保留。新结果只写草稿。

## 本次验证记录（2026-09-14）

- Worker 非付费测试：414 passed，1 个付费集成用例明确排除；覆盖快分片先评分、修正失败恢复而不重生成、身份/数量校验、原样保留、主线/六维/正文同步及 429 降并发。
- `mypy src`、Worker 源码及新增测试 Ruff、`git diff --check` 通过。
- Prompt Worker Docker 镜像构建成功；确认没有排队或运行中的 Prompt Run 后更新并重启，仅替换 Prompt Worker。运行配置核验为 AI 并发 4、评分输出预算 8192，消费者已开始正常监听。
- 上述自动回归与部署阶段没有发起付费生成；用户随后授权“提交后自行付费测试”，已提交 `437a592` 并经正式浏览器发起一轮实际广式腊肠项目 100 条 × 15 秒生成。未执行视频渲染或完成校验。

## 真实付费对比（2026-09-14）

同商品、同上游 revision 2，输入指纹与批次设置哈希完全相同；模型未更换。新批次没有复用基线生成结果。期间 API 重启导致一次连接失败与同 Run 恢复，总耗时包含这部分成本。

| 指标 | 改造前 | 本轮 |
| --- | ---: | ---: |
| 端到端后台耗时 | 36.51 分钟 | 17.97 分钟 |
| 最终数量 | 100 | 100 |
| 实际生成候选 | 120 | 100 |
| 语义重复度 | 14% | 11% |
| 硬拒绝 / 完全重复 | 0 / 0 | 0 / 0 |
| 视觉表达清晰度（AI） | 94.10 | 93.75 |
| 产品相关性（AI） | 94.94 | 94.94 |
| 创意连贯性（AI） | 93.76 | 93.34 |
| 画面可执行性（AI） | 89.32 | 89.42 |
| 商业素材价值（AI） | 90.51 | 89.24 |
| 独立 AI 画面修正调用 | 旧式评分后修订路径 | 25 组全部成功 |

本轮省时约 50.77%，但不能把全部收益归因于并发：基线另有 20 条多样性补充，本轮未触发补充。单轮结果不能证明稳定达到此速度或质量完全不退步。

### 调用、恢复与浏览器

- 11:47:50 开始，12:05:49 后台完成。约第 6 分钟已同时存在生成和评分分片；浏览器正确显示两个执行中节点与部分评分结果。
- Worker 日志记录 108 次调用开始、101 次成功响应；包含格式恢复与 API 中断时取消的调用，不等于供应商结算笔数。
- 11:54:52 出现 `InternalApiError`，底层为连接失败；API 监听进程启动时间为 11:54:51，与开发服务重启吻合。任务自动进入第 2 次尝试，已保存分片恢复成功。不是模型幻觉或结构门禁淘汰。
- 日志记录 4 次供应商结构响应 `json_invalid` 提醒，以及 2 次评估分片拆分恢复；最终全部 25 个生成分片与 25 个评分分片成功。未新增付费批次。
- 运行中页面重开恢复同一任务成功。最终刷新遇到白屏，控制台报告模板混剪页面热更新加载失败；额外 Web 类型检查发现该未提交模块导出缺失、契约及函数接口不一致。保留其他工作改动，未伪称终态浏览器验收通过。
- 本次提交后的 Worker 非付费回归再次通过。没有因真实测试改变模型、模板或放宽任何门禁。

### 人工质量复查

最终按 P001、P006……P096 等间隔抽查 20 条，另核对已发现风险的最终编号。以下是文本层面的风险，不是实际渲染故障率：

| 编号 | 尚存问题 | 通用问题类别 |
| --- | --- | --- |
| P026 | 一条 15 秒动作链内依次切三种形态并分区整理，且固定机位又水平跟移 | 动作预算、镜头冲突 |
| P031 | 一双筷子的左右两根分别夹住两种食材，没有形成可行夹持结构 | 工具接触与支撑 |
| P036 | 多片圆形厚片要求无支撑直立；取景略低于盘沿还需看清前排细节 | 稳定性、遮挡 |
| P032 | 用细长厨刀轻划完整竹筒即劈开，缺少预切开口或合理支撑 | 材料阻力、动作难度 |
| P046 | 水平跟随向上抬勺，固定机位又向前探 | 相对运动与运镜矛盾 |
| P076 | 同一双筷子同时夹排骨与薄片，旋转后还要两种断面对齐 | 多物体精细操作 |
| P006 | 静置融化奶酪要求呈现丝缕，却没有拉伸驱动 | 材料状态与运动来源 |
| P096 | 从空盘连续逐片码到铺满整盘，15 秒内节奏偏密 | 首帧完成度、动作预算 |

本轮模型对执行难度与镜头冲突的警告计数下降，但人工仍发现明确漏检，且商业素材价值评分下降 1.27 分。因此结论是“速度与重复度改善，画面修正仍需优化”，不能以 `PASS` 声称视频画面无故障。

收到的 30 条卖点均保留；本轮 AI 将 25 条作为画面实现目标、5 条作为上下文，目标缺失 0；基线为 26 + 4。目标口径由每轮 AI 策略决定，所以“缺失 0”不等于 30 条均被画面证明。

### 后续建议（未实施）

保留本次流水调度。优先增强独立 AI 修正调用对“已有明确冲突但原样放过”的识别，要求给出具体可执行替换；不新增 Worker 语义规则，不靠淘汰候选或把所有镜头改静态解决。下一次验证应跨品类对比同样的工具接触、支撑、材料状态、遮挡与时间预算，持续监测商业价值和重复度。

完整结果、对比 JSON 与运行日志保存在 Git 忽略的 `.tmp/prompt-speed-20260914/`，本文仅保留脱敏指标与安全摘要。
