# 效果类工作流——差异化 Prompt 批量生成节点实施方案

- 当前状态：已完成
- 最后更新时间：2026-08-27
- 原型基准：`references/prototypes/effect/effect-workflow.html` 中第 03 步差异化 Prompt 批量生成区域

## 1. 目标与实施范围

将现有第 03 步从浏览器 `localStorage` 与前端规则 Mock 升级为真实的异步子工作流闭环。保留四项批次设置、产品切换、质量统计、Prompt 列表、编辑、单条重生成、服务端导出和十条分页，并增加可恢复、可点击的子工作流进度弹窗。

本次包含共享契约、JSON Schema、Prisma 迁移、NestJS 公开与内部 API、独立 Python LangGraph Worker、RabbitMQ/Outbox、前端接入、WorkingArtifact 提交边界、部署配置和测试。

本次不实现片段渲染、Seedance、混剪、时间轴精修和成片导出，也不重构 AI 信息提炼业务。工作区内既有 AI 信息提炼未提交修改必须保留。

## 2. 已确认业务口径

- 一次 Run 只处理当前产品；不提供一键全部生成。
- 数量范围 10～200，默认 50。
- 每条保存叙事、场景、人物、卖点、镜头和情绪完整六维；程序优先选择至少三个维度不同的方案，但总数量和六类配额优先，普通相似只计入整批指标。
- 核心卖点轮动覆盖；模型不得创造硬事实、功效或信任背书。
- 语义重复度默认不超过 15%，视觉结构化重合度默认不超过 20%。首版使用确定性结构化代理算法，不增加 Embedding 依赖。
- 全量重生成保留人工编辑和人工新增项，再补足目标数量；单条重生成只替换目标项。
- 生成、补齐、重生成和人工编辑只更新领域结果与节点草稿。只有当前产品“完成校验”后才提交 `prompt-batch:{productId}` WorkingArtifact。
- 达到三轮补齐上限仍未通过时保留 `NEEDS_REVIEW` 草稿，不允许提交下游。

## 3. 子工作流与持久化

公开拓扑固定为：

```text
LOAD_AND_SNAPSHOT
  → STRATEGY_PLANNING
  → DIMENSION_COMBINATION
  → CANDIDATE_GENERATION
  → NORMALIZATION
  → [SEMANTIC_DEDUP || VISUAL_DEDUP]
  → QUALITY_GATE
  → REPLENISH（必要时回到候选生成，最多三轮）
  → RESULT_SAVE
```

LangGraph state 只保存项目 ID、轮次、分片 ID 与计数；正文和完整结果通过受保护内部 API 持久化到 PostgreSQL。Worker 使用独立队列、Token、claim/lease/heartbeat 和 attempt token；RabbitMQ 消息只携带运行标识。

新增 `EffectPromptRun`、`EffectPromptStageOutput`、`EffectPromptShardOutput` 与 `EffectPromptResult`。结果区分 `generatedResult`、`draftResult` 和 `manualOverrides`，写入使用 revision/CAS。WorkingArtifact 依赖当前营销洞察 revision 与产品级 Prompt 设置执行哈希。

## 4. 接口与质量规则

公开 API 统一位于：

```text
/projects/:projectId/workflows/effect/prompt-generation
```

提供工作区、产品结果分页/搜索、启动批量或单条 Run、Run/节点详情、人工新增修改删除、完成校验和权威 JSON 导出。内部 Worker API 位于 `/internal/workers/effect-prompt-generation`。

语义代理以可变正文的中文字符 3-gram Dice 和结构化内容意图签名判重；视觉代理按场景 35%、人物 20%、镜头 30%、情绪 15% 加权判重。批次指标为违规 Prompt 对占全部 Prompt 对的比例。系统先剔除高度相似或六维差异不足候选，再按缺口 1.25 倍补齐。

## 5. 测试与验收标准

- Contracts：Schema、拓扑、范围与非法数据。
- NestJS：项目隔离、事务 Outbox、幂等、活动 Run 唯一、租约、防旧 attempt、CAS、人工覆盖、提交门禁与脱敏。
- Worker：六维距离、卖点覆盖、分片、确定性质量、三轮补齐、部分失败、严格 Ark Schema 与 Mock。
- 前端：加载/空态/失败、刷新恢复、产品/项目切换、子图、编辑器六维、搜索分页、键盘、窄屏和 409。
- 门禁：`pnpm check`、Worker `pytest/mypy`、Compose config/build 和浏览器回归。真实 Ark 仅在用户另行授权且 Key 可用时执行。

## 6. 实施记录

2026-08-25 完成实施：

- 新增共享 TypeScript 契约、严格 JSON Schema、公开子图拓扑与设置/结果/API 类型；前端、NestJS 和 Python Pydantic 已统一字段口径。
- 新增 Prisma 迁移 `20260825170000_create_effect_prompt_generation`，建立 Run、Stage、Shard、Result、租约、幂等和产品级活动任务唯一约束；迁移已在本地 PostgreSQL 成功执行。
- 新增 NestJS 公开 API 与受保护 Worker API，实现事务 Outbox、CAS、项目隔离、双重质量重算、人工覆盖、单条原位重生成、提交门禁和事务内依赖复核。
- 新增独立 `workers/effect-prompt-generation`，实现 LangGraph `Send` 分片、最多三轮补齐、Ark 严格 Schema、显式 Mock、断点恢复、运行期缓存清理与安全日志。
- 第 03 步前端已移除 Prompt `localStorage` Mock，接入服务端工作区、设置保存、轮询恢复、分页搜索、六维编辑器、单条重生成、服务端导出和安全化子图详情；片段渲染及后续节点未改动。
- `pnpm check` 通过：Contracts 12 项、Web 119 项、API 161 项测试全部通过，lint、Prettier、TypeScript 与生产构建通过。
- Worker `uv run --frozen pytest` 19 项通过，`uv run --frozen mypy src tests` 22 个源文件通过；Compose 配置校验和 Worker 镜像构建通过。
- 浏览器使用显式 Mock Provider 完成 50 条批量生成、刷新恢复、10 条分页、安全节点详情、单条原位重生成、窄屏弹窗、ESC 与焦点恢复回归；截图见 `docs/browser-regression/effect-prompt-subworkflow-completed.png` 与 `docs/browser-regression/effect-prompt-subworkflow-narrow.png`。回归结果仅保存在领域草稿，未点击“完成校验”，未提交 Prompt WorkingArtifact。
- 未执行真实 Ark 付费测试；后续需在用户明确授权且本机 Key 可用时另行执行。

## 7. 2026-08-25 Prompt 质量与节点详情整改（已被第 9 节纠正）

### 7.1 变更原因

浏览器回归发现，现有候选生成模板及 Mock 只产出一段式创意概述，虽然包含六维标签，但缺少可直接供视频素材生成消费的时间轴分镜、主体动作、产品细节、镜头执行、光线质感、声音/字幕及负向约束。与此同时，公开节点详情仅投影批次数量、分片、剔除和补齐等少量通用字段，多数节点没有对应 metadata，导致右侧详情缺少可用于理解和验收的信息。

> 纠正记录：本节曾错误地把单条 Prompt 设计成覆盖完整时长的多镜头视频方案，与“一条 Prompt → 一个素材片段 → 后续混剪”的正式链路冲突。第 9 节为当前实施口径；本节仅保留为变更历史，不再作为验收依据。

### 7.2 本轮目标与范围

- 保持公开拓扑、批次设置、结果表和 WorkingArtifact 提交边界不变。
- 将候选生成内部 Ark Schema 升级为结构化素材生成方案：每条包含创意核心、分时段镜头、画面与主体动作、产品聚焦、景别/运镜、光线质感、声音或字幕以及负向约束；Worker 再确定性拼装最终中文 Prompt。
- 策略规划和候选生成 Prompt 增加高质量正例、低质量反例与逐项自检要求。示例只作为结构与详略度基准，不得复制示例中的产品事实。
- Mock Provider 使用与真实 Provider 相同的分镜结构和最终拼装路径，避免浏览器验收被低质量规则文本误导。
- 拆分模型路由：策略规划使用独立轻量模型覆盖项，候选生成保留 Seed 2.1 Turbo；分别增加最大输出 Token 和最小推理强度配置。默认候选模型仍回退 `ARK_PROMPT_MODEL/ARK_MODEL`，运行失败不得静默切换模型。
- 节点详情按节点白名单展示真实安全摘要：输入快照、维度池规模与示例、正交组合示例、候选结构/分片进度、标准化结果、双重去重指标、门禁结论、补齐缺口和保存结果。允许展示已标准化的业务示例，不展示模型标识、内部 Prompt 模板、原始响应、Token 明细、存储位置或内部标识。
- 前端详情布局复用上一个 AI 信息提炼节点的状态、更新时间、刷新、字段卡片和加载/错误反馈模式；保持冻结原型的蓝白弹窗、左右分栏和窄屏结构。

本轮不修改片段渲染、Seedance、混剪、导出、相似度算法或数据库表结构，也不执行真实 Ark 付费调用。

### 7.3 数据、接口与部署影响

- Python 内部模型新增分镜草稿结构；跨服务 `EffectPromptItem.content` 仍为字符串，已有结果兼容，不新增数据库迁移。
- `EffectPromptStageOutput.metadata` 继续保存 JSON，但 Worker 只写安全化计数、短文本示例和质量值；公开 API 依据 `nodeId` 二次白名单投影。
- 新增部署项：`ARK_PROMPT_STRATEGY_MODEL`、`ARK_PROMPT_CANDIDATE_MODEL`、`ARK_PROMPT_STRATEGY_MAX_OUTPUT_TOKENS`、`ARK_PROMPT_CANDIDATE_MAX_OUTPUT_TOKENS`、`ARK_PROMPT_REASONING_EFFORT`。旧 `ARK_PROMPT_MODEL` 继续作为兼容回退。

### 7.4 测试与验收标准

- Worker：严格分镜 Schema、示例模板版本、确定性 Prompt 拼装、产品名/指定卖点/时间轴/镜头/负向约束完整性、节点级模型与 Token 参数、Mock/Ark 契约一致。
- NestJS/Contracts：每个公开节点只返回允许字段；未知 metadata、模型、模板、原始响应、Token 和内部 ID 不得泄露。
- 前端：逐个点击 10 个节点，右栏均有与阶段匹配的说明或明确等待态；支持刷新、加载、错误、更新时间、长文本换行、窄屏、ESC 和焦点恢复。
- 执行 `pnpm check`、Worker `pytest/mypy`、Compose config/build 和浏览器回归；未获额外授权不执行真实 Ark。

## 8. 2026-08-25 Prompt 列表操作完整性整改

### 8.1 当前节点与上下游边界

- 当前属于效果类工作流第 3 个业务节点“Prompt 生成”，直接读取第 2 节点已完成校验的 `marketing-insight:{productId}` WorkingArtifact；产品级批次设置保存在 `PROMPT_GENERATION:{productId}` WorkflowNodeState，并作为执行输入快照的一部分。
- 本节点领域结果仍为 `EffectPromptResult`；正式提交粒度仍为 `prompt-batch:{productId}`。人工新增、修改、删除、单条重生成和批量重生成只更新领域草稿与节点状态，不在操作按钮点击时提交 WorkingArtifact。
- 只有用户点击“完成校验”且数量、六维差异、双重去重和上游依赖均通过时，才更新 `prompt-batch:{productId}` WorkingArtifact。有效内容哈希变化后由既有工作副本机制在提交边界传播下游 STALE；未校验编辑不提前影响第 4 节点。
- 单条重生成继续使用 Prompt 稳定 `item.id` 和 `code` 原位替换；下游只消费完成校验后的批次、稳定 ID、内容哈希和依赖快照，不读取页面临时状态。
- 本轮明确不开发第 4 节点视频渲染、Seedance、模板混剪、成片输出，也不修改 AI 信息提炼、相似度算法、模型 Prompt 或数据库结构。

### 8.2 本轮目标与实施范围

- 逐项验真截图中的搜索、人工添加、修改、复制、删除、单条重新生成和批量导出，沿用现有公开 API、服务端分页与 revision/CAS，不新增生产依赖。
- 删除操作增加二次确认、ESC 关闭、焦点恢复和明确忙碌状态，避免误删或重复提交。
- 区分删除与单条重生成的进行中状态，阻止同一时刻重复点击或交叉触发，避免两个按钮同时显示错误的加载态。
- 复制优先使用 Clipboard API，并为浏览器权限或非安全上下文提供本地降级复制；复制不修改节点草稿。
- 批量导出继续从服务端权威结果生成 JSON，不从当前分页或搜索结果拼装；生成任务执行期间禁止导出旧快照。
- 完善前端静态交互断言和服务层/API 测试，覆盖按钮接线、CAS、权威导出及单条原位重生成。

### 8.3 测试与验收标准

- Prompt 专项前端与后端测试全部通过；新增交互必须通过 TypeScript、ESLint 和 Prettier。
- 删除确认支持确认、取消、遮罩、ESC 与焦点恢复；确认期间不能重复提交。
- 复制成功或降级失败均给出安全中文反馈；搜索仍由服务端在 ID、内容、片段类型和六维标签上过滤并保持 10 条分页。
- 单条重生成只替换目标项并保留稳定 ID、code、顺序和其他条目；批量导出包含服务端完整权威批次，不受当前页和搜索词影响。
- 完成后在本节记录真实测试、构建和页面回归结果，不以计划值代替执行结果。

### 8.4 实施与验收结果

- 当前状态：已完成。搜索、人工添加、修改、复制、删除、单条重新生成和批量导出继续复用服务端权威结果、项目隔离与 revision/CAS；没有新增生产依赖，也没有触碰第 4～6 节点。
- 删除按钮已增加二次确认、遮罩/ESC 取消、确认中防重复提交和触发按钮焦点恢复；删除与单条重生成改为独立忙碌类型，不再让两个按钮同时显示加载态或交叉重复触发。
- 复制按钮优先使用 Clipboard API，权限或非安全上下文不可用时降级到本地选区复制；失败时只显示安全中文提示，不修改节点草稿。
- 人工添加在服务端读取结果正文前先执行 200 条上限保护，避免为上限批次做不必要的全量质量重算，也不会写出超出共享 Schema 的无效草稿。
- 人工添加和批量导出仅在当前 V2 权威结果已经成功载入时启用；旧 V1、结构无效、加载中或正在生成时保持禁用，批量导出仍包含服务端完整批次而非当前分页/筛选结果。
- V2 设置恢复增加旧快照兼容填充，缺失的新字段使用共享默认值并深拷贝数组/权重，进入页面不再因 `additionalDisabledElements` 缺失而崩溃。
- Prompt 专项测试通过：Web 4 个文件 21 项、API 5 个文件 39 项、Worker 29 项及 mypy 25 个源文件；仓库 ESLint、TypeScript、Contracts 16 项、Web 123 项、API 180 项和前后端生产构建均通过。任务相关文件 Prettier 检查通过；仓库总 `pnpm check` 仅被任务开始前已修改的 `AGENTS.md` 格式告警中断，未擅自改写该用户文件。
- 浏览器回归确认 Prompt 工作区可从旧设置快照恢复；旧 V1 结果会明确要求重新生成，且“人工添加提示词”“批量导出”均为禁用态。第 9 节 Worker V2 源码测试与类型检查已通过，但运行中的容器尚未用该并行改造重建、当前项目仍只有旧 V1 结果，因此本轮未触发真实/Mock 批量生成，也没有写入或提交新的 `prompt-batch:{productId}` WorkingArtifact。

## 9. 2026-08-25 素材片段 Prompt 生成质量方向纠正

### 9.1 当前状态与业务边界

- 当前状态：已完成。
- 当前节点为效果类第 3 节点“Prompt 生成”，只读取最新已提交的 `marketing-insight:{productId}`，产出 `prompt-batch:{productId}`。
- 正确链路为“一条 Prompt → 一个可独立渲染的素材片段”；50 条 Prompt 代表 50 个素材片段方案，不代表 50 条最终成片。
- 六维差异只用于内部组合、分配和去重，必须自然转译成可见场景、单一人物、连续动作、产品关系、单一运镜、光线与节奏，不得在最终正文中输出“创意核心、差异化设定、叙事=、时间轴镜头”等策划元话语。
- 片段标签和六维作为结构化元数据单独保存，不混入可复制的最终视频生成 Prompt。
- 本轮不实施片段渲染、Seedance、模板混剪、时间轴或最终成片节点。

### 9.2 模型与生成契约

- 候选模型严格只返回 `slotId + promptText`；标签、六维、卖点、画幅和目标时长由冻结组合确定性回填。
- 每条 `promptText` 只描述一个场景、一个主要主体、一个连续可见动作和一种主要运镜；禁止多镜头时间轴、完整广告弧、多场景转场和受众画像堆叠。
- 七类主标签固定为钩子、痛点、产品展示、效果展示、卖点讲解、结尾转化、片尾品牌。正文不输出标签名，只执行该标签的单一素材职责。
- 策略规划增加卖点可见证据模式；无来源素材支持的工艺、配方、技术和抽象品质不得伪造成生产过程或效果证明，只能转为合规卖点讲解。
- 策略节点继续使用 Doubao Seed 2.0 Lite，候选节点继续使用 Doubao Seed 2.1 Turbo；其余阶段保持确定性。候选输出上限由 6144 调整为 4096 Token。

### 9.3 执行门禁与接口影响

- 在双重去重前增加执行有效性校验，拒绝元话语、完整时间轴、人物堆叠、缺少可见动作、不可拍证据、片段职责冲突、重复句、事实越界和占位错词。
- Normalization 只做空白、标点和受控枚举规范化，不自动改写或掩盖低质量模型结果；无效候选按原标签和卖点定向补齐。
- 人工新增和编辑执行相同门禁，并返回稳定、可理解的原因码和中文提示。
- 结果 Schema 升级到 V2；旧 V1 完整视频结果保留只读但禁止完成校验，必须重新生成。
- 节点详情展示各阶段业务示例、执行无效数量和安全原因汇总，不展示模型标识、系统 Prompt、原始响应、内部 ID 或存储位置。

### 9.4 测试与验收标准

- 用户提供的完整视频反例必须被元话语、人物堆叠、完整时间轴和不可拍证据规则拒绝。
- 七类片段分别满足单一职责；痛点和部分钩子片段允许不出现产品。
- 最终正文中六维均体现为可执行画面信息，但不得出现六维字段名、内部标签或差异化说明。
- 无生产素材时不得生成工厂、实验室、腌制过程、检测设备或专家背书。
- 50 条批次继续满足标签配额、核心卖点覆盖和双重重复度阈值；六维至少三项不同作为择优目标和告警，不再逐对硬淘汰。
- 执行 Contracts、NestJS、Vue、Worker pytest/mypy、Compose 和浏览器回归；真实 Ark 仍需用户另行明确授权。
- 完成后在本节记录真实迁移、测试、构建、截图和提交结果，不以计划值代替执行结果。

### 9.5 最终实施与验收结果

- 此前“完整多镜头视频方案/时间轴 Prompt”方向已废弃。候选模型现在严格只返回 slotId + promptText，Worker 按冻结任务回填七类标签、六维、时长和卖点；最终正文不再拼接策划标题、字段键值或完整广告结构。
- 策略规划已按可见证据分级。工艺、配方、技术、品质、配比、口感、香味、无淀粉和锁鲜等抽象事实默认只能进入卖点讲解；效果片段只使用可见属性、单一使用动作或来源已确认的可见结果。没有过程素材时不会生成工厂、实验室、生产过程或消费者“演示工艺”。
- 候选模板已加入四类可直接执行的高质量正例和低质量反例；每条限定 120～600 字、一个场景、一名主要主体、一个连续动作和一种主要运镜。Worker 只规范空白、标点并追加简短可见禁项，不再拼装标题、字段名、时间轴或重复的长合规尾段。
- 执行门禁已覆盖策划元话语、多镜头时间轴、受众画像堆叠、缺少可见动作、不可拍证据、职责冲突、机械重复、事实越界和占位破损文本；无效候选按原标签/卖点定向补齐。V1 结果保持只读并禁止提交；V1 的完整成片时长在首次 V2 生成时原子迁移为默认 5 秒素材片段时长，避免把旧 15 秒配置截断后误用。
- 根据 2026-08-25 最新确认，页面已删除“风格覆盖、七类素材标签配比、卖点权重、追加禁用元素”四块用户配置。七类标签仍由系统内部按固定配额分配，卖点自动轮动，上游禁用项继续强制继承；页面只保留数量、统一片段时长、语义阈值和视觉阈值四项必要设置。
- 显式 Mock 端到端回归实际生成 50 条、统一 5 秒的素材片段 Prompt：七类配额为 8/7/9/9/8/5/4，核心卖点覆盖 3/3，执行无效 0，语义重复度 0.0%，视觉结构重合度 0.0%，质量状态为 PASS，“完成校验”门禁已解锁。
- 对最终数据库正文做实际审计：50 条长度为 213～306 个中文字符；“差异化/差异表达/创意核心/时间轴/视频生成方案/补齐策略/叙事字段”泄漏 0 条；“25-45 岁受众集合/全国消费者/家庭厨房决策者”人物堆叠泄漏 0 条。“广府糖酒腌制工艺”仅出现在卖点讲解片段，以真实切面配合原文短字幕表达，并明确禁止虚构工厂、实验室或生产过程。
- 浏览器回归确认：设置区只剩四项必要参数；列表正文与主标签/次级标签/六维元数据分离；50 条分页、固定标签筛选、质量统计、卖点覆盖和十节点安全详情均可恢复。回归使用显式 PROMPT_AI_PROVIDER=mock，未执行真实 Ark 付费测试。
- 最终自动验证通过：Contracts 16 项、Web 125 项、NestJS API 181 项、Worker pytest 30 项；TypeScript/Vue 类型检查、ESLint、前后端生产构建、Worker mypy（25 个源文件）、Compose 配置解析和 Worker 镜像构建均通过。任务文件 Prettier 与 git diff 空白检查通过；仓库已有的 AGENTS.md 和 AI 信息提炼页面修改未纳入本功能提交。
- 本次仍未开发视频渲染、Seedance、模板混剪、时间轴和最终成片节点。

## 10. 2026-08-26 六类片段独立配置与条件 Prompt 路由 V3

### 10.1 当前状态与纠正原因

- 当前状态：已完成。
- 第 9 节的七类固定配额与统一片段时长已经被本节取代；“效果展示片段”从公开契约、配置、生成、编辑、筛选、统计和质量门禁中彻底删除，不改名或并入其他类型。
- V3 固定六类素材片段：钩子、痛点、产品展示、卖点讲解、结尾转化、片尾品牌。默认数量为 `10/8/12/10/6/4`，合计 50 条；六类默认时长均为 5 秒。
- 本轮仍只修改效果类第 3 节点“Prompt 生成”，直接读取 `marketing-insight:{productId}`，产出 `prompt-batch:{productId}`；不开发视频渲染、Seedance、模板混剪和最终成片。

### 10.2 数据、路由与交互口径

- Schema 升级为 V3。批次设置以六类 `fragmentConfigs` 分别保存数量和时长，数量合计限制为 10～200、每类至少 1 条，时长限制为 3～10 秒；语义和视觉阈值继续作用于全批次。
- 已取消的风格覆盖、标签权重、卖点权重和追加禁用元素从 V3 设置契约删除；视觉基线和强制禁用项继续继承信息卡。
- V2 设置按默认六类比例迁移数量并复制合法统一时长；V1/V2 结果不在 V3 工作区展示、不参与人工项保留或提交，只在数据库审计链保留并要求重新生成。
- 子工作流增加确定性的“片段类型条件路由”，将同类型分片分别发送到六个候选生成分支。六类使用代码版本化的独立系统提示词，Ark Responses API 通过 `instructions` 传入系统指令，用户输入只携带安全产品上下文、冻结组合和输出任务。
- 设置选择器同时驱动结果列表筛选。选择具体类型时编辑其数量和时长；选择“全部片段”时仅显示数量汇总和统一值/多值时长摘要。
- 修改任一类型配置后，旧结果只标记 `STALE`，不会自动改写正文；人工新增和编辑的时长由所选类型配置确定，不再单条覆盖。

### 10.3 测试与验收标准

- Contracts/API：V3 Schema、六类枚举、总量边界、按类型时长、设置哈希、V2 设置升级、旧结果阻断和 CAS。
- Worker：同类型分片、六分支条件路由、系统模板映射、按类型补齐、单条重生成、断点恢复、最大并发 3 和严格 Ark Schema。
- Prompt 质量：六类单一职责、抽象卖点安全表达，以及元话语、完整时间轴、受众堆叠和虚构证据拒绝。
- Web：全部汇总、六类切换、独立加减、总量上限、自动保存、列表联动、六分支子图、窄屏和键盘交互。
- 显式 Mock 浏览器回归必须生成 50 条，分布为 `10/8/12/10/6/4`、全部 5 秒、不含效果展示类型并达到 PASS；未经授权不运行真实 Ark 付费测试。
- 完成后在本节补充真实测试、构建、浏览器验收和 Git 提交结果，不以计划值代替实际结果。

### 10.4 实施与验收结果

- 共享契约、严格 JSON Schema、NestJS DTO、Python Pydantic 与 Vue 类型均已升级到 Schema V3；`EFFECT_DEMONSTRATION` 已从六类枚举、设置、结果、编辑、筛选、统计、质量校验和子图中删除。V2 设置会按六类默认比例迁移，V1/V2 结果仅保留审计且不能参与 V3 CAS、人工保留或提交。
- 六类 `fragmentConfigs` 已成为唯一数量和时长来源。全部视图只读展示 50 条与统一 5 秒/多值摘要；具体类型可分别调整数量和 3～10 秒时长，同一个选择器同步筛选列表和统计。浏览器实测切换“卖点讲解片段”后显示独立数量 10、时长 5 秒及 10 条结果。
- LangGraph 已加入确定性片段类型条件路由和六个 `Send` 分支，分片保持单一类型、每片最多 8 条、全图最大并发 3。六类系统模板独立版本化，每套含公共事实/安全基线、至少两个合格示例和一个职责冲突反例；Ark 候选响应仍严格限制为 `slotId + promptText`，策略规划与候选模型分层保持不变。
- 组合器修复了“六类循环序号与卖点数量存在公因数时只命中同一卖点”的问题，改为按片段类型内部序号轮询抽象卖点；Mock 产品展示也服从冻结组合动作，不再在补齐轮次重复同一句展示动作。Worker 与 NestJS 的可见动作词表已统一识别自然表达“摆到”。
- API 修复了旧 V1/V2 审计结果阻断新 V3 Run 的 CAS 问题：新任务只以最新合法 V3 结果作为基础 revision，旧结果继续保留但不进入工作区或执行快照。
- 显式 Mock 浏览器回归生成 50 条素材片段 Prompt，配额为 `10/8/12/10/6/4`，全部 5 秒，不含效果展示类型；核心卖点覆盖 3/3，执行无效 0，语义重复度 0.0%，视觉结构重合度 0.0%，服务端质量状态为 `PASS`，“完成校验”按钮已解锁。六类设置与六分支子图截图见 `docs/browser-regression/effect-prompt-v3-six-fragment-settings.png`、`docs/browser-regression/effect-prompt-v3-six-branch-workflow.png`。
- 最终自动验证通过：Contracts 18 项、Web 125 项、NestJS API 185 项、Worker pytest 31 项；ESLint、Prettier、TypeScript/Vue 类型检查、前后端生产构建、Worker ruff/mypy、Compose 配置解析和 Worker 镜像构建均通过。
- 浏览器验收期间首次容器错误继承了本机 `PROMPT_AI_PROVIDER=ark`，意外发起两次 Ark Responses 请求并因策略 JSON 截断失败，未生成或提交结果。发现后立即强制重建为显式 `mock` 并核验容器环境，后续全部浏览器验收均使用 Mock。该事实保留在实施记录中，不以 Mock 结果冒充真实 Ark 验收。
- 未开发视频渲染、Seedance、模板混剪、时间轴和最终成片节点；用户已有的 `AGENTS.md` 与 AI 信息提炼修改未纳入本功能提交。

## 11. 2026-08-26 子工作流节点真实执行结果展示整改

### 11.1 当前状态与范围

- 当前状态：已完成。
- 本轮只整改效果类第 3 节点“Prompt 生成”的子工作流右侧详情；不修改 Prompt 生成算法、六类配置、视频渲染或后续混剪节点。
- 详情直接读取已有 Run 的不可变输入快照、阶段输出、分片结果和最终领域结果，历史 V3 Run 无需重新生成。
- 每个内容型节点最多展示 3 条真实样例；详情保持只读，编辑、复制和重生成仍由 Prompt 主列表负责。

### 11.2 契约、数据与交互口径

- 公开节点详情增加类型化内容块，覆盖真实标签集合、正交组合、六类路由、候选 Prompt、语义/视觉重合对和质量问题；原有状态、摘要、指标、告警与安全错误继续保留。
- NestJS 使用节点详情专用查询读取 `inputSnapshot + StageOutput + ShardOutput + Result`，普通 Run 轮询不加载分片正文。
- 语义和视觉样例从持久化候选分片按既有确定性算法重新定位，Stage 计数仍为权威执行指标；运行中的节点只展示已完成分片，空态不生成固定示例。
- 前端删除“执行依赖”和固定业务示例，将详情栏扩展为紧凑指标、标签组、组合卡、Prompt 卡和对比卡；Prompt 与对比卡可用键盘展开全文。
- 公开投影不返回模型、系统 Prompt、原始响应、Token、内部 ID、存储位置、attempt 信息、URL、Base64 或本地路径。

### 11.3 测试与验收标准

- Contracts 覆盖内容块结构、三条样例上限和非法数据拒绝；API 覆盖全部 16 个节点、项目隔离、历史 V3、运行中/空/失败状态与安全脱敏。
- 去重详情样例必须真实达到语义或视觉阈值，并与 Worker 的阶段统计口径一致。
- Web 覆盖真实标签、组合、Prompt、对比展开、切换防串写、滚动、窄屏、键盘、ESC 和刷新。
- 浏览器使用当前广式腊肠 V3 Run 验收，不调用真实 Ark；详情必须出现真实品类、真实六维组合、实际候选 Prompt、实际去重结果和最终 50 条保存结果。
- 完成后在本节补充真实测试、构建、浏览器截图和提交结果。

### 11.4 实施与验收结果

- 公开节点详情契约已增加 `TAG_LIST / COMBINATION_LIST / ROUTE_LIST / PROMPT_LIST / PAIR_LIST / ISSUE_LIST` 六种类型化内容块，统一限制为每个内容型节点最多 3 条样例。普通 Run 轮询仍只查询 Stage 和 Result，仅节点详情专用查询加载 Shard 正文。
- NestJS 已从历史 V3 Run 的真实 `inputSnapshot + StageOutput + ShardOutput + Result` 完成安全投影。实测直接恢复出产品“广式腊肠”、品类“腊味肉制品”、核心卖点、六类 `10/8/12/10/6/4` 配置、真实正交组合、六类候选 Prompt、语义重复对与最终 50 条保存结果，全程未重新生成也未调用 Ark。
- 去重详情使用与 Worker 一致的中文 3-gram Dice、意图签名和视觉四维权重重新定位违规对。当前历史批次实际展示 3 组达到 0.82 阈值的语义相似 Prompt，最高一组为 88.78%；视觉违规对为 0 时明确保持空结果，不伪造对比样例。
- 公开投影已对 URL、Data URL、长 Base64、Windows 本地路径和内部字段执行白名单脱敏；不返回模型、系统 Prompt、原始响应、Token、slot/run/attempt 等内部标识或存储位置。
- Vue 子工作流已删除“执行依赖”和固定业务示例，右侧详情扩展到 420px，支持真实标签组、组合卡、路由统计、Prompt 全文和去重对比的键盘展开；1120px 以下改为上下布局。节点、产品或弹窗切换时会取消旧请求并清理旧内容。
- 历史 V3 Run 中不存在后续 V4 新增阶段数据时，该两个阶段保持“等待中/暂无实际产出”，旧的 16 个阶段仍全部可查，不用假数据填充新阶段。
- 定向验证通过：Contracts 11 项、节点投影与专用查询 23 项（含当前全部公开节点逐一投影）、Service 白名单 1 项、Web 布局 11 项，Vue TypeScript 检查和 NestJS 生产构建通过。当前工作树另一项尚未完成的 V4 任务仍有旧 Fixture/断言未升级，因此 API 全量 typecheck/全量测试仍由该独立任务阻断；本次真实详情代码、定向测试和生产构建均已通过。
- 浏览器实测完成输入快照、候选 Prompt 全文展开和语义对比展开；截图见 `docs/browser-regression/effect-prompt-real-input-detail.png`、`docs/browser-regression/effect-prompt-real-prompt-detail.png`、`docs/browser-regression/effect-prompt-real-semantic-pair.png`。

## 12. 2026-08-26 提炼信息深度利用与 Prompt 子工作流 V4

### 12.1 当前状态与业务边界

- 当前状态：已完成。
- 本轮只改造效果类第 3 节点“Prompt 生成”，直接读取已提交的 `marketing-insight:{productId}`，继续产出 `prompt-batch:{productId}`；不增加用户可见业务节点，不开发视频渲染、模板混剪和成片输出。
- 当前 V3 已完整传递信息卡，但策略阶段把受众、痛点、场景、卖点等拆成独立候选池，正交组合破坏了业务关系，质量门禁也只强制核心卖点覆盖。本轮改为“事实映射 → 营销关系规划 → 片段蓝图编排 → 六类生成 → 信息覆盖校验 → 定向补齐”。

### 12.2 数据、生成和门禁口径

- Schema 升级为 V4。每条 Prompt 保存来自已提交信息卡的稳定事实绑定；批次保存强制、已覆盖、缺失、自适应暂未使用事实和已应用全局约束。V1～V3 结果只保留审计，设置继续无损迁移。
- 产品身份、核心规格、视觉特征、全部核心卖点/痛点/决策驱动、目标受众、营销目标和每个非空场景组的首个代表场景为强制事实；次要卖点、剩余场景及确认可用的价格/背书为自适应事实；空值和“需确认、待核实、未知”等不确定内容显式排除。
- 六类片段使用固定职责映射。模型只能引用 Worker 提供的事实 ID；Worker 校验未知引用、职责冲突和敏感事实误用，六维差异只在同一营销关系束内展开，不再跨无关事实随机拼接。
- 去重后按实际保留条目的合法绑定计算覆盖。强制事实缺失会进入最多三轮定向补齐并阻断 PASS；自适应事实未使用只展示“暂未编排”。单条重生成保留原事实绑定，人工编辑按正文/类型/六维变更范围重新验证绑定。
- 生成和编辑仍只更新领域结果与 `WorkflowNodeState`；只有用户完成校验且 V4 门禁通过时才更新 WorkingArtifact。继续记录上游 artifact、revision、contentHash 和输入指纹，STALE 以有效内容变化为准。

### 12.3 接口、界面和安全口径

- 公共子图新增 `INSIGHT_MAPPING` 与 `INSIGHT_COVERAGE`，现有策略、组合和补齐节点改为用户可理解的业务名称；节点详情只返回安全化事实数量、覆盖情况、排除原因和补齐目标。
- 主页面增加“提炼信息利用情况”，展示核心覆盖、待补、自适应暂未使用和已应用约束；每条 Prompt 展示精简来源类型标签，不暴露模型、系统 Prompt、原始响应、内部 JSON 或存储信息。
- 继续复用现有 HTTP 路径、JSON 结果字段和 CAS，不新增 Prisma 表、不做数据库迁移、不增加生产依赖。

### 12.4 测试与验收标准

- Contracts、NestJS、Pydantic 和 Worker 严格对齐 V4 Schema；信息卡所有非空字段必须进入强制、自适应或排除集合，未知事实 ID 和职责冲突必须被拒绝。
- 六类分支分别验证允许和禁止内容；“需确认”价格不得进入 Prompt，背书只能按原文用于卖点讲解。
- 核心事实缺失触发定向补齐，三轮后仍缺失为 `NEEDS_REVIEW`；人工新增、修改、删除、复制、单条重生成后覆盖统计正确。
- 使用广式腊肠完整信息卡执行显式 Mock 端到端验收：默认 50 条必须覆盖全部核心卖点、核心痛点、决策驱动、受众和营销目标，各场景组至少覆盖一个代表场景，不确定价格不得出现。
- 完成后在本节记录真实测试、构建、浏览器验收和 Git 提交结果，不以计划值代替实际结果。

### 12.5 实施与验收结果

- 共享 TypeScript 契约、严格 JSON Schema 与 Python Pydantic 已升级到 V4。`EffectPromptItem` 现保存经过来源和职责校验的 `insightBindings`，批次指标保存强制、已覆盖、缺失、自适应暂未编排、排除事实和已应用制作约束；原卖点覆盖继续由绑定后的核心卖点兼容投影产生。V1～V3 领域结果保持审计只读，合法旧设置会迁移到 V4；未新增 Prisma 表、数据库迁移或生产依赖。
- Worker 已新增确定性的提炼事实映射与覆盖计算。事实 ID 由字段和规范化安全值生成，不确定字段显式进入排除集合；价格只允许在结尾转化中按确认原文使用，可信背书只允许在卖点讲解中作为证据原文使用。候选模型的完整产品上下文不再暴露未分配的价格和背书。
- 策略规划现返回引用事实 ID 的营销关系束，组合器先分配关系束，再在束内变化叙事、人物、镜头、情绪和动作。Ark 和 Mock 都必须返回 `usedFactIds`；Worker 会逐束拒绝未知事实、重复引用、片段职责冲突及敏感事实误用，再从确定性事实映射补齐被拒绝关系束造成的覆盖缺口。质量门禁以去重后实际保留条目的合法绑定计算覆盖，强制事实缺失会驱动最多三轮定向补齐，仍缺失时保存为 `NEEDS_REVIEW` 而不能完成校验。
- 人工新增会对正文和六维做确定性事实匹配；只改正文保留原绑定并标记人工编辑；修改片段类型会移除不兼容绑定并重算覆盖；删除条目会立即暴露其唯一承担的核心事实缺口。单条重生成优先保留目标条目的关系束和事实 ID，无绑定条目优先领取该类型可承载的未覆盖事实。
- Prompt 主页面已增加“提炼信息利用情况”，展示核心覆盖、待补信息、自适应暂未使用信息和全局约束；每条 Prompt 只展示“核心痛点、使用场景、核心卖点”等安全来源标签。子工作流增加“提炼信息应用映射”和“提炼信息覆盖校验”，规划、编排和补齐节点使用新的业务名称，详情只公开统计、缺失原因和补齐目标。
- 广式腊肠完整信息卡的显式 Mock 图测试实际生成默认 50 条，六类配额保持 `10/8/12/10/6/4`，全部强制事实均由职责正确的片段覆盖，三个非空场景组均覆盖代表场景，`需确认` 价格进入排除集合且未出现在任何正文中，最终质量状态为 `PASS`。随后真实 Ark 运行结果记录在 12.6。
- 最终自动验证通过：Contracts 21 项、NestJS API 196 项、Prompt 页面专项 25 项、Worker pytest 34 项；TypeScript/Vue 全量类型检查、ESLint、前后端生产构建、Worker mypy/定向 ruff、Compose 配置解析和 Worker 镜像构建均通过。仓库全量 Web 测试仅有 1 项失败，来自任务开始前已有的 AI 信息提炼页面测试与 `vite.config.ts` 未提交修改口径不一致，不属于 V4 文件；本任务文件的 Web 测试全部通过。
- 浏览器回归确认当前商品和查看工作流按钮位置、顶部批次设置、空态及 V4 页面加载正常，控制台无错误。页面级 Mock 任务因本机已运行 API 与重建 Worker 的内部 Token 不一致，在输入快照前返回 401；该回归任务已精确标记失败并保留审计，没有调用 Ark、没有生成结果，也没有提交 WorkingArtifact。因此覆盖面板的结果态以组件测试和完整 Mock 图测试验收，不将该环境失败冒充浏览器生成成功。
- 生成、人工编辑和覆盖重算仍只更新领域草稿；只有用户点击“完成校验”且 V4 质量门禁通过才提交 `prompt-batch:{productId}`。未修改效果类第 4～6 节点、定制类或裂变类工作流；本节与 V4 代码使用同一次 Git 提交交付。

### 12.6 2026-08-26 真实 Ark 运行阻塞修复与验收

- 当前状态：已完成。故障最初表现为 Run 长时间停留在“等待 Prompt 生成服务接单 / 0%”。根因是本地 Compose Worker 使用默认 Token，而开发 API 在缺少 `.env` 显式配置时没有使用同一默认值，claim 返回 401，消息被拒绝后 Run 保持 `QUEUED`。
- 开发环境的两个 Worker Token 默认值已与 Compose 对齐；生产环境仍强制显式配置，不降低生产鉴权要求。原 Run 的精确 Outbox 消息经状态核对后安全重放，claim 恢复为 201。
- Compose 中策略规划输出上限此前仍为 `2048`，与 Worker、`.env.example` 和文档的 V4 默认 `8192` 不一致，导致关系规划 JSON 在闭合前被截断。Compose 默认已统一为 `8192`，重建容器后真实规划一次返回 4657 输出 Token 并成功进入蓝图编排。
- 对严格 Schema 通过但业务绑定不完整的模型结果增加确定性保护：卖点证据漏项按信息卡原文降级为 `TEXT_ONLY`；含未知 `factId`、重复引用或职责冲突的关系束逐条丢弃；缺失的强制事实和六类职责只从 Worker 已确认事实映射中补齐，不接受模型补造事实。
- Worker 全量 pytest 34 项、mypy 和定向 ruff 通过；API 环境配置测试 7 项和 TypeScript typecheck 通过，Worker 镜像已重建并确认运行时 `ARK_PROMPT_STRATEGY_MAX_OUTPUT_TOKENS=8192`。
- 广式腊肠真实 Ark Run `48649da9-694a-4505-9f00-8d68e7c6c4e7` 已完成：页面从 0% 依次进入营销关系规划、六类生成、去重、覆盖门禁和三轮定向补齐，最终保留 45 条可用 Prompt；22/22 项核心事实、3/3 个确认卖点和 17 项制作约束均已正确覆盖。钩子缺 2 条、卖点讲解缺 3 条，因此按既定规则保存为 `NEEDS_REVIEW`，未提交 `prompt-batch:{productId}` WorkingArtifact，也未绕过门禁自动进入第 4 节点。

## 13. 2026-08-26 精确数量与 Seedance 参数分离 V5

### 13.1 当前状态与范围

- 当前状态：已完成。
- 本轮改造效果类第 3 节点“Prompt 生成”，并实现第 4 节点必要的 Seedance 请求编译边界；不实现真实异步渲染、媒体绑定、模板混剪或成片输出。
- 直接上游仍为已提交且 CURRENT 的 `marketing-insight:{productId}`；第 3 节点继续产出 `prompt-batch:{productId}`，第 4 节点只消费该已提交批次及其稳定渲染配置快照。

### 13.2 数量、正文与渲染参数口径

- 成功生成的批次必须同时满足总数量和六类片段配额。三轮模型定向补齐后仍有缺口时，使用已校验营销关系束和片段蓝图进行确定性安全兜底；若兜底仍不能通过事实、职责、去重和执行门禁，则任务失败且不得保存短批次覆盖旧结果。
- Schema 升级为 V5。批次增加结构化 `renderProfile`，保存标准化画幅、分辨率、模型能力键及批次级禁用元素；每条 Prompt 的 `targetDurationSeconds` 继续作为独立字段。
- Prompt 正文只描述主体、场景、动作、产品表达、镜头、光线、情绪和结束状态，不再出现时长、画幅、分辨率、批次禁用元素或通用合规尾句。
- Seedance 请求编译器把 `duration`、`ratio`、`resolution` 作为独立 API 字段传递；官方没有独立负面提示词字段，因此统一禁用元素只在服务端提交任务前去重并追加一次，不回写领域 Prompt。
- 默认能力为 Seedance 2.0（4～15 秒），通过安全能力键适配 2.0 fast、1.5 和 1.0 差异；不向前端暴露模型 Endpoint、密钥或内部部署信息。

### 13.3 兼容、界面和测试标准

- V4 历史结果保持审计可读且不原地改写；新生成产生 V5。旧 3 秒设置在按 Seedance 2.0 重新生成时迁移为 4 秒并显示明确提示。
- 页面集中展示统一画幅、分辨率、模型时长范围和统一禁用元素；单条卡片、复制和正文编辑只处理干净 Prompt。导出时将正文、时长、画幅、分辨率和禁用元素拆成独立字段。
- TypeScript、JSON Schema、Pydantic、Worker、NestJS 和 Vue 必须严格对齐；覆盖模型漏项、无效候选、重复候选、六类精确配额、安全兜底、V4 兼容、Seedance 能力差异、请求编译和人工编辑后数量门禁。
- 完成后在本节记录真实测试、构建、浏览器验收和 Git 提交结果，不以计划值替代实际结果。

### 13.4 实施与验收结果

- Prompt 批次契约已升级到 V5，增加 `renderProfile` 与 `fallbackCount`。信息提炼结果同步携带分辨率，使第 3 节点仍只读取直接上游已提交信息卡即可冻结画幅、分辨率和统一禁用元素；未新增 Prisma 表、数据库迁移或生产依赖。
- Worker 不再要求正文包含片段秒数、画幅或分辨率，也不再主动追加统一禁用元素和通用合规尾句；候选模型指令明确要求正文不要复述这些批次参数，并把统一禁用元素作为避让画面的上下文提供给模型。技术参数或统一禁用项偶尔出现在正文时不淘汰候选、不阻止人工编辑或完成校验，渲染仍以结构化 `renderProfile` 和请求编译结果为准。投放渠道与视觉基线只作为创意上下文，不以字段标签形式进入正文。
- 三轮模型定向补齐后会按六类实际/目标数量逐类生成确定性安全变体，并继续经过事实来源、片段职责、可见动作、镜头执行性、六维距离、语义去重、视觉去重和信息覆盖门禁。Worker 保存与 NestJS 完成接口均执行总数和六类配额硬校验；无法安全补齐时任务失败，不会覆盖上一份有效结果。
- 页面新增“统一渲染设置”，集中展示画幅、分辨率、模型允许时长和统一禁用元素；规则兜底数量仅出现在批次概览。人工增删后会同时显示逐类“缺少/超出”数量并阻断完成校验。复制只取干净正文，JSON 导出把正文、时长、画幅、分辨率和禁用元素拆为独立字段。
- 第 4 节点边界新增纯 Seedance 请求编译器：只消费已提交 V5 批次，将 `duration`、`ratio`、`resolution` 作为独立请求参数，并把去重后的共享禁用元素仅追加一次到发送快照。能力适配器覆盖 Seedance 2.0、2.0 fast、1.5 Pro 和 1.0；2.0 fast 收到 `1080p` 等不支持配置时返回明确错误，不静默降级。查询结果校验会对比任务快照中的时长、画幅和分辨率。
- V4 结果通过内存只读投影保持可查看，数据库原结果不被改写；重新生成才创建 V5。旧 3 秒结构化配置会调整为 Seedance 2.0 最低 4 秒，并在旧结果状态提示中明确说明。
- 缺口验收使用 Mock Provider 连续三轮生成包含“9:16、1080p、5 秒”的无效正文，所有模型候选均被门禁拒绝；安全兜底最终得到目标 10/10 条且六类逐类精确一致，正文不含这些技术参数。默认 50 条路径继续验证为精确 `10/8/12/10/6/4`。
- 自动验证实际通过：Contracts 22 项、NestJS API 208 项、Prompt Web 专项 27 项、Prompt Worker pytest 38 项、Extraction Worker pytest 59 项（另 3 项显式集成测试跳过）；两个 Worker mypy 均无错误；全仓 ESLint、TypeScript/Vue 类型检查和生产构建通过。全仓 Web 测试仍有 1 项既有信息提炼页面断言与用户未提交的 `vite.config.ts` 源码别名改动不一致，本任务 Prompt Web 专项全部通过。
- 本次没有调用真实 Ark 或 Seedance，没有实现完整异步渲染、媒体参考图、音频、水印、模板混剪或成片输出；正式渲染仍由效果类第 4 节点后续实现。

## 14. 2026-08-26 单条 Prompt 可视化定向重新生成

### 14.1 当前状态与范围

- 当前状态：已完成。
- 本轮只改造效果类第 3 节点列表中的单条重新生成能力。全量批量生成、普通人工编辑、第 4 节点渲染及后续混剪保持现状。
- 单条 Run 继续使用 `ITEM_REGENERATE`，只替换目标条目；保留稳定 ID、编号、位置、片段类型、时长和次级标签，其他条目不得变化。

### 14.2 交互与生成口径

- 点击“重新生成此条”先打开可视化弹窗，不立即发请求、不自动打开工作流。弹窗展示原 Prompt，并以 3×2 卡片呈现叙事、场景、人物、卖点、镜头和情绪六维。
- 六维预填原值，修改项显示原值到新值和“已调整”状态；除卖点外允许安全自定义，卖点只能选择当前已提交信息卡中与片段职责兼容的确认值。修改意见最多 500 字且允许留空。
- 请求新增可选 `regenerationInstruction` 与完整 `replacementDimensions`。后端将两者写入不可变快照和输入指纹，不新增 Prisma 表、不提升结果 Schema 版本；旧客户端未提交六维时沿用原值。
- Worker 以用户六维冻结唯一替换组合，卖点变化时同步重绑确认事实。候选模型只能在冻结六维、事实和安全边界内改写正文，不增加第三次模型调用。
- 保存时强制保留目标 ID、编号、位置、片段类型、时长和标签，更新正文、六维及必要的提炼绑定；完成前仍执行整批执行门禁、事实覆盖、六维距离和语义/视觉去重。

### 14.3 测试与验收标准

- Web 覆盖弹窗开关、六维卡片、修改高亮、恢复、意见计数、空意见提交、目标行状态、ESC、焦点恢复和窄屏。
- Contracts/API 覆盖字段边界、批量任务拒绝重生成字段、卖点白名单、CAS、幂等、项目隔离和历史快照兼容。
- Worker 覆盖冻结六维、卖点事实重绑、意见安全注入、补齐重试，以及其他条目完全不变、目标稳定身份和位置不变。
- 使用显式 Mock 完成浏览器回归，不调用真实 Ark；完成后在本节记录实际测试、构建和验收结果。

### 14.4 实施与验收结果

- 共享请求契约与 NestJS DTO 已增加可选 `regenerationInstruction` 和完整 `replacementDimensions`。批量任务携带任一单条字段会返回 400；修改意见去空白后限制为 500 字，六维执行完整性、非空和字段长度校验。
- 单条 Run 的不可变输入快照和来源指纹已覆盖原条目、原位置、用户六维和修改意见。卖点由服务端根据当前信息卡及片段职责执行白名单校验：产品展示和转化只允许确认核心卖点，卖点讲解允许确认核心/次要卖点，钩子与痛点只允许保留原安全中性表达。
- Worker 单条路径只规划一个冻结组合，补齐重试继续使用同一用户六维。候选模型收到原 Prompt、低优先级修改意见和锁定字段，不得覆盖片段类型、时长、次级标签、事实或安全约束；卖点变化会重新绑定对应的确认事实，未增加第三次模型调用。
- 结果合并已强制保留目标 ID、code、createdAt、列表位置、片段类型、时长和次级标签，目标恢复为 AI 条目并清除人工编辑状态；正文、六维和合法提炼绑定按新候选更新，其他条目保持原对象和顺序。
- Vue 列表按钮已改为“重新生成此条”。弹窗实际展示当前 Prompt、条目编号/类型/时长/标签、3×2 六维卡片、原值到新值、已调整计数、恢复操作、卖点下拉白名单、五维搜索/自定义建议及 500 字修改意见。无修改时按钮为“直接换一版”，存在修改时为“按当前设置重新生成”；确认后不自动打开工作流。
- 浏览器在当前广式腊肠结果上完成只读回归：点击 P001 只打开单条弹窗，初始为 `已调整 0/6`；选择“场景代入型”并填写修改意见后变为 `已调整 1/6` 和“按当前设置重新生成”。ESC 关闭后焦点恢复到原“重新生成此条”按钮；720px 视口下六维卡片为单列、底部操作为纵向布局。全程未点击确认按钮，未调用 Ark。
- 实际验证通过：Contracts 13 项；Prompt API 59 项；Prompt Web 26 项；Worker pytest 38 项；Worker mypy 15 个源文件无错误；Contracts/API/Web TypeScript 类型检查、NestJS 生产构建和 Vue/Vite 生产构建均通过。Worker 请求消息版本同步对齐 Schema V5，避免 V5 Outbox 消息被旧 Literal 拒绝。为与同工作树 V5 契约对齐，同时补齐了既有测试 Fixture 的 Schema V5、renderProfile、fallbackCount 与信息卡 resolution 字段，不改动其业务逻辑。
- 全仓 `pnpm lint`、全工作区 typecheck 和 production build 均通过。全仓测试仍有 2 个与本功能无关的既有信息提炼断言未对齐当前工作树（Vite contracts 源码直连方式及生产规则覆盖 Fixture）；全仓 format check 仍被用户已有的 `AGENTS.md`、资产概览、工作流节点 ID 和 V5 Seedance 编译器未格式化修改阻断，本功能涉及文件均已单独 Prettier 校验。
- 本轮未修改全量批量生成、普通人工编辑、真实视频渲染、模板混剪和最终成片；未运行真实 Ark 付费测试，未新增数据库表、迁移或生产依赖。

## 15. 2026-08-26 视频素材 Prompt 质量 V4 整改

### 15.1 当前状态与业务边界

- 当前状态：已完成。
- 本轮只整改效果类第 3 节点“Prompt 生成”的策略提示词、六类候选提示词、事实表达范围与确定性执行门禁；直接上游仍为已提交的 `marketing-insight:{productId}`，领域输出仍为 Schema V5 的 `prompt-batch:{productId}`。
- “质量 V4”是生成模板版本，不回退或提升领域 Schema，不新增公开 API、Prisma 表、数据库迁移或生产依赖。
- 不开发参考图绑定、真实 Seedance 渲染、字幕口播、模板混剪和最终成片；未经明确授权不调用真实 Ark 或 Seedance。

### 15.2 调研结论与新口径

- Seedance、Runway 与 Veo 官方指南均强调以主体运动为核心，再补充场景、单一镜头运动、光线和风格；短素材应避免复杂时间线、多场景和过度描述。
- 最终正文统一为“场景与首帧状态 → 一个连续动作弧 → 一个主景别/机位/运镜 → 必要光线与材质 → 清楚结束状态 → 可选真实环境声”的自然段，不再按字段逐项复述。
- 保留 `120～600` 字符兼容边界，推荐正文压缩到 `160～280` 个中文字符。时长、画幅、分辨率和统一禁用元素继续通过结构化渲染参数传递，不写回领域 Prompt。
- 原始素材采用纯净画面策略：禁止烧录字幕、口播、BGM、价格、二维码和促销角标；结尾转化与片尾品牌仅预留干净安全区。`TEXT_ONLY / PROCESS_ONLY` 卖点保留为结构化元数据，只生成匹配的真实产品细节素材，不伪造工厂、实验室或生产过程。
- 每条确定性选择一个主要表达事实和最多两个职责兼容的辅助事实；其余事实只用于规划、安全和批次覆盖。模型返回的 `usedFactIds` 必须精确等于实际表达事实子集，最终条目不再保存未影响正文的绑定。
- 模型分层保持不变：策略规划使用 Doubao Seed 2.0 Lite，六类候选使用 Doubao Seed 2.1 Turbo；不增加第三次模型审查，继续保持每片最多 8 条、全图最大并发 3。

### 15.3 执行门禁与验收标准

- 在现有元话语、完整时间线、职责冲突、来源事实和基础可执行性规则上增加：动作过载、运镜冲突、事实过载、烧录文字、音频越界、抽象信息伪造、物理跳变、无参考图精确还原和重复负向尾段。
- 门禁失败后按原片段类型、卖点、六维和表达事实定向补齐，不由 Normalization 自动改写正文；公开详情只展示安全化中文原因。
- 建立食品、日用品和设备三类共 18 组质量样本，并覆盖六类片段、抽象卖点、高风险事实、单条重新生成和补齐路径。
- 默认 50 条验收必须保持六类配额、事实覆盖、六维差异及语义/视觉重复度门槛，同时不得出现字段说明、完整广告结构、烧录文字、口播/BGM、动作过载和互相冲突的运镜。
- 完成后在本节补充实际 pytest/mypy、关联 Contracts/NestJS/Vue 验证、构建、Token 统计和验收结果，不以计划值代替真实结果。

### 15.4 实施与验收结果

- 策略规划模板升级为 `effect-prompt-strategy-v5`，通用候选模板升级为 `effect-prompt-candidate-base-v5`、任务模板升级为 `effect-prompt-candidate-task-v6`，六类职责模板均升级为 v2。新模板固定使用“场景与首帧 → 一个动作弧 → 一种主运镜 → 必要光线材质 → 结束状态”的纯净素材结构，并明确禁止字幕、口播、旁白、BGM、价格贴纸、二维码、完整广告弧和长负向尾段。
- 组合器会从合法营销关系束中按片段职责和未覆盖事实确定性选择 1～3 项实际表达事实；Ark 严格响应仍返回 `slotId + promptText + usedFactIds`，但 `usedFactIds` 现在只允许等于该表达子集。单条重新生成沿用相同选择规则，人工编辑的服务端事实推断也按职责优先级限制为最多 3 项。
- Worker 与 NestJS 同步新增动作过载、运镜冲突、事实过载、烧录文字、音频越界、抽象证据伪造、物理跳变、无参考图精确还原和重复负向尾段门禁。TEXT_ONLY / PROCESS_ONLY 卖点不再强制出现在正文，只保留结构化绑定并生成职责匹配的真实产品细节素材；节点详情和页面统计均增加安全化中文原因。
- 新增食品、日用品、设备三类 × 六种片段职责共 18 组质量样本。显式 Mock 50 条批次实测正文长度为 184～216 个中文字符，P50 为 200.5、P95 为 213；每条最多 3 项事实，14/14 项强制事实仍全部覆盖，输出不含字幕、口播、旁白、BGM、二维码和价格贴纸。
- 实际验证通过：Prompt Worker pytest 61 项、mypy 15 个源文件、涉及文件 ruff check/format；Contracts 22 项；NestJS API 全量 213 项及 TypeScript 类型检查；Prompt Web 专项 27 项及 Vue TypeScript 类型检查；NestJS 与 Vue/Vite 生产构建均通过。全 Worker 目录 ruff 检查仍报告 10 个任务开始前已存在的无关导入排序问题，本次涉及的 9 个 Python 文件全部通过。
- 未调用真实 Ark 或 Seedance，因此没有可代表生产模型的真实 Token P50/P95；候选输出上限保持不变，以上字符长度只作为 Mock 成本代理，不冒充真实 Token 数据。未开发参考图绑定、真实渲染、字幕口播、模板混剪和最终成片。

## 16. 2026-08-26 六类素材片段 Prompt 专项质量整改

### 16.1 当前状态与业务边界

- 当前状态：已完成；真实付费验收已完成，六类中 4 类通过、2 类暴露出需继续优化的生成缺口。
- 本轮继续整改效果类第 3 节点“Prompt 生成”，直接读取已提交的 `marketing-insight:{productId}`，继续产出 Schema V5 的 `prompt-batch:{productId}`；不改变公开 API、结果字段、Prisma 表或 WorkingArtifact 提交边界。
- 本轮只扩展 Worker 内部策略结构、六类候选模板、组合冻结信息、确定性门禁和安全化详情原因；不开发第 4 节点异步渲染、参考图绑定、字幕口播、模板混剪或最终成片。
- 历史已完成 V5 结果继续只读展示；部署时未完成的旧策略结构 Run 不与新结构混合恢复，应安全失败并提示重新生成。

### 16.2 调研结论与六类口径

- Seedance 官方指南强调明确主体、动作、场景和镜头控制；Runway 建议 5～10 秒片段保持单场景、聚焦运动和直接表达；Veo 推荐按镜头、主体、动作、环境和氛围组织 Prompt；TikTok Creative Codes 的 Hook、Body、Close 只用于界定素材职责，不用于生成完整广告。
- 当前六类 v2 模板只有职责说明和两个示例，策略规划仍让六类共享全局叙事、动作、镜头与情绪池，现有门禁也不能稳定识别“钩子已揭晓、痛点已解决、产品未首帧出现、证据模式错配、转化缺少留白、片尾仍有剧情”等分支错误。
- 六类模板升级为 v3，并分别维护首帧模式、单一动作弧、镜头方案、结束状态、禁止事项和跨品类示例：钩子保持悬念；痛点保持未解决；产品展示形成清楚英雄构图；卖点讲解只呈现允许证据；结尾转化形成安全留白；片尾品牌形成极少动作的稳定定格。
- Prompt 改为时长自适应长度：4～5 秒为 80～150 字，6～8 秒为 110～200 字，9～15 秒为 140～260 字。Ark 严格响应边界调整为 80～260 字，历史正文不按新长度原地改写。

### 16.3 策略、门禁与验收口径

- 策略模型继续使用 Doubao Seed 2.0 Lite，但必须为六类分别输出首帧、动作弧、镜头和结束状态池；确定性组合器只从目标片段类型的池中取值，并继续限制为一个主动作、一种主运镜和最多三个职责相关事实。
- 候选模型继续使用 Doubao Seed 2.1 Turbo，六类各走独立系统 Prompt；不增加第三次模型审查。新增分支门禁覆盖钩子提前揭晓、痛点被解决、产品首帧缺失或混入效果、卖点证据模式错配、转化缺少安全区、片尾动作/新信息过载以及时长对应正文长度错误。
- 自动测试覆盖食品、日用品和设备三类 × 六类职责的 18 组正向样本，并为六类补充职责冲突、动作过载、运镜冲突、证据错配和结束状态错误；50 条批次继续通过配额、事实覆盖、六维距离和双重重复度门禁。
- 用户已授权真实验收：当前选中产品调用 1 次策略规划和六类各 1 次候选生成，共预计 7 次 Ark 请求；再提交 6 条 Seedance 2.0 标准模型任务，统一 5 秒、9:16、720p、无参考素材并关闭生成音频。创意结果失败不自动追加付费任务。
- 实施完成后在本节记录自动测试、构建、真实 Ark 请求与 Token 统计、6 个 Seedance 任务状态和逐类画面验收结论；不得记录密钥、模型原始响应或内部 attempt 标识。

### 16.4 实施与首轮真实验收结果

- 六类策略结构已从共享动作/镜头池改为六类独立池，每类分别冻结首帧、单一动作弧、主镜头、情绪和结束状态；旧结构的运行中分片不再与新规划混合恢复。六类候选继续走独立模板，其中钩子、痛点和产品展示为 v3，卖点讲解、结尾转化和片尾品牌在首轮真实验收后针对暴露的问题升级为 v4。
- Prompt 长度已改为随片段时长自适应：4～5 秒 80～150 字、6～8 秒 110～200 字、9～15 秒 140～260 字；Worker 严格响应范围同步为 80～260 字。组合器只使用目标类型对应的策略池，并继续冻结一个动作弧、一种主运镜和最多三个相关事实。
- Worker 与 NestJS 已同步六类专属门禁，包括钩子提前揭晓、痛点同条解决、产品未在首帧建立主体、卖点证据模式不匹配、转化缺少安全留白、片尾未稳定或引入新信息，以及正文长度与时长不匹配。节点详情仅投影安全化中文原因。
- 自动验证实际通过：Prompt Worker pytest 76 项、mypy 15 个源文件；Contracts 22 项及构建；NestJS API 全量 217 项、质量专项 19 项、TypeScript 类型检查和生产构建；Prompt Web 全量 147 项、Vue TypeScript 类型检查和生产构建。食品、日用品和设备三类 × 六类的 18 组正向样本以及职责冲突、动作过载、运镜冲突、证据错配、结束状态错误和旧分片恢复均已覆盖。
- 首轮真实验收严格按授权执行 7 次 Ark 请求且未自动重试：策略规划 1 次，输入 11,195 Token、输出 5,852 Token、耗时约 161.2 秒；六类候选各 1 次，合计输入 17,392 Token、输出 1,084 Token，单次耗时约 5.2～6.6 秒。钩子、痛点、产品展示通过；卖点讲解因证据模式不匹配被拒绝，结尾转化因人物画像堆叠被拒绝，片尾品牌因缺少可见微动作和产品锚点被拒绝。
- 针对真实失败已完成确定性修正：抽象卖点不再信任模型自行选择证据模式，统一降为安全的 `TEXT_ONLY / PROCESS_ONLY`；人物规划把受众集合收敛为单一明确出镜者；卖点模板强制对齐允许证据，转化模板要求首句使用准确产品名且不写受众身份，片尾模板要求产品锚点、可观察的环境微动和明确停止状态。修正后全部自动化测试通过，但未擅自再次发起付费 Ark 请求。
- 因首轮只有 3/6 类通过执行门禁，未创建任何 Seedance 付费任务，避免把已知不合格 Prompt 提交渲染。第二轮需要重新执行 1 次策略规划、六类各 1 次候选生成；全部通过后再创建 6 个 Seedance 标准任务。该轮属于新的付费请求，必须取得用户再次明确授权，不会自动追加。
- 用户授权第二轮后，首次策略请求已成功，但临时验收执行器因未传入后续新增的批次级 `sharedPrompt` 参数而在候选调用前退出；该策略响应遵守安全规则未持久化，用户随后明确授权补做 1 次策略调用。执行器已补齐参数并通过语法检查。
- 补做后的真实策略请求输入 5,579 Token、输出 7,341 Token、耗时约 192.6 秒；六类候选合计输入 20,971 Token、输出 1,165 Token，单次耗时约 5.2～6.7 秒。痛点、产品展示、卖点讲解和片尾品牌通过；钩子因缺少明确镜头执行语句被 `MISSING_CAMERA_EXECUTION` 拒绝，结尾转化因 5 秒正文超过 150 字被 `PROMPT_LENGTH_MISMATCH` 拒绝。
- 针对第二轮真实缺口，钩子模板升级为 v4，强制用完整短句表达“主景别 + 机位 + 唯一运镜”；结尾转化模板升级为 v5，4～5 秒严格限制 80～150 字、最多三个紧凑句子且安全留白只描述一次。修正后 Worker pytest 76 项与 mypy 再次通过。四条已通过候选仅暂存在本机验收临时文件，不写入仓库或应用日志。
- 由于第二轮仍为 4/6 通过，尚未创建 Seedance 任务。下一步只需补做 1 次策略规划和钩子、结尾转化各 1 次候选生成；两条通过后即可与已通过的四条一起提交 6 个 Seedance 任务。继续付费调用前仍需再次取得用户明确授权。

### 16.5 最终真实视频验收结果

- 用户再次授权后，仅补做 1 次策略规划和钩子、结尾转化各 1 次候选生成。策略请求输入 5,579 Token、输出 6,821 Token、耗时约 212.0 秒；两类候选合计输入 7,077 Token、输出 365 Token，单次耗时约 6.3～6.5 秒。钩子直接通过；结尾转化已真实写出“连续空白区域、干净无遮挡”，原门禁因同义词词表缺失误报，Worker 与 NestJS 同步扩展安全区识别后使用原文重新校验通过，未增加 Ark 请求。
- 六类候选全部通过文本门禁后，按授权创建且仅创建 6 个 Seedance 2.0 标准任务：统一 5 秒、9:16、720p、无参考素材、关闭音频和水印。6 个任务全部成功，实际均为 720×1280、24fps、约 5.04 秒、无音轨；合计视频生成用量 653,400 completion Token。完成结果已立即下载到本机 `D:\1Code\day3\ai-video-workflow\seedance-output`。
- 技术 QA 全部通过：六条均无黑帧、超过 1 秒的异常冻结、烧录字幕、价格、二维码或其他可读营销文字。抽取首帧、中帧和末帧完成逐类画面检查，验收联系图保存在输出目录的 `qa-20260826-final` 子目录。
- 六类画面结论：痛点通过，问题状态保持未解决，场景从菜市场偏移为厨房操作台但未破坏职责；卖点讲解通过，切片抬起、逆光纹理和推近动作连续；结尾转化通过，产品稳定且右侧留白可供后续混剪；片尾品牌通过，静物收焦后稳定停留。钩子未通过，末帧已能识别整根腊肠，悬念保留不足；产品展示未通过，画面从真空袋装直接变为蒸笼散装，缺少开袋和取出动作，物理连续性断裂。
- 真实失败已经回写规则：钩子模板升级为 v5，要求结束时仍覆盖主体约四分之三，只露出不可独立辨认的小段纹理；产品展示模板升级为 v4，要求首尾维持同一种包装/裸露状态且动作必须解释结束状态。Worker 与 NestJS 新增“袋装状态在无打开/取出动作时跳到蒸笼、盘中、切片或散装状态”的 `PHYSICS_BREAK` 检测。
- 最终修正后自动验证通过：Prompt Worker pytest 79 项、mypy 15 个源文件；NestJS API 全量 221 项、TypeScript 类型检查和生产构建；此前已通过的 Contracts 22 项及 Prompt Web 147 项保持不变。根据“创意失败不自动追加付费任务”的约束，没有为钩子和产品展示创建第二轮 Seedance 任务。

## 17. 2026-08-26 批次共用提示词双层应用

### 17.1 当前状态与范围

- 当前状态：已完成。
- 本轮仍属于效果类第 3 节点“Prompt 生成”，并仅修改第 4 节点必要的 Seedance 请求编译边界；直接上游仍为已提交的 `marketing-insight:{productId}`，领域输出仍为 `prompt-batch:{productId}`。
- 批次只维护一份可扩展的“共用提示词”：生成候选时作为模型约束上下文，创建视频任务时由服务端追加一次；任何阶段都不得把它写回单条 `EffectPromptItem.content`。
- 不新增效果类顶部业务节点、Prisma 表、数据库迁移、生产依赖或完整视频渲染任务。内部子流程节点改为“共用提示词编译”，首期编译系统禁用元素与用户补充内容，未来允许增加其他批次级段落。

### 17.2 契约、子图与界面

- `EffectPromptBatchResult` 增加兼容性可选 `sharedPrompt`，包含版本、来源段落、最终编译内容和内容哈希。首期段落为系统维护的 `DISABLED_ELEMENTS` 与用户可编辑的 `USER_ADDITIONAL`；`renderProfile.sharedConstraints.prompt` 停止作为新结果的权威字段，仅保留历史 V5 读取兼容。
- 子工作流顺序调整为 `输入快照 → 提炼信息应用映射 → 共用提示词编译 → 营销关系规划`。候选模型收到共用提示词并被要求遵守但不复述；正文偶尔出现相关文字时不因该原因单独淘汰。
- Seedance 请求编译器只读取批次 `sharedPrompt.compiledContent` 并追加一次，同时独立传递时长、画幅和分辨率；历史 V5 缺少 `sharedPrompt` 时才从结构化禁用元素生成兼容内容。
- Prompt 主页面继续隐藏六类配额、卖点覆盖和提炼利用组件，只在结果区展示一次紧凑的“共用提示词”面板。系统禁用段可查看，用户补充段可编辑，单条复制保持纯净正文，批量导出把共用提示词作为批次字段单独输出。

### 17.3 测试与验收标准

- Contracts、JSON Schema、NestJS 解析器与 Pydantic 必须严格对齐；历史 V5 无 `sharedPrompt` 结果继续可读，新结果固定生成批次共用提示词和稳定哈希。
- Worker 测试验证共用提示词进入候选模型输入但不写回单条正文，空值、重复禁用元素、用户补充内容和图节点顺序均正确。
- API 测试覆盖用户补充内容保存、revision 冲突、重新进入待校验状态、WorkingArtifact 提交边界、历史兼容和导出；Seedance 编译器验证同一共用提示词只追加一次。
- Web 测试覆盖结果区查看、编辑、保存、loading/error、窄屏和复制/导出边界；完成后记录真实测试、类型检查、构建结果并将本节状态更新为“已完成”。

### 17.4 实施与验收结果

- 契约、JSON Schema 与 Pydantic 已同步批次级 `sharedPrompt`；新批次固定生成系统禁用元素段和用户补充段，历史 V5 结果仍可读取旧 `renderProfile.sharedConstraints.prompt`，不会原地伪造新字段。
- Worker 已增加“共用提示词编译”内部节点，并把同一份编译结果传入六类候选模型。候选模板明确要求遵守但不复述，共用内容只保存在批次结果，不写回单条正文，也不因正文偶尔出现约束文字直接淘汰候选。
- NestJS 已增加带 result revision 并发校验的共用内容保存接口；编辑只更新领域结果并恢复为待校验状态，只有再次完成校验才更新 `prompt-batch:{productId}` WorkingArtifact。单条新增、编辑、删除和重生成均保留批次共用提示词。
- Seedance 请求编译器已改为 `item.content + sharedPrompt.compiledContent`，空内容不追加，并在请求快照记录共用提示词哈希；旧 V5 结果仅在缺少 `sharedPrompt` 时从禁用元素临时编译，已移除并行维护第二套尾句的逻辑。
- Prompt 页面已上线单一“共用提示词”面板：系统段只读、用户段可编辑、合并结果即时预览；单条复制保持纯净正文，批量导出把共用提示词作为批次字段单独输出，未恢复六类配额和提炼覆盖概览。
- 自动验证实际通过：Contracts 22 项；NestJS API 全量 219 项；Prompt Web 全量 147 项；Prompt Worker pytest 77 项、mypy 15 个源文件与 Ruff 规则检查。Contracts、API、Web 的 TypeScript 类型检查和生产构建均通过。
- 浏览器验收已在当前“广式腊肠”结果页完成：桌面与 720px 窄屏布局无横向溢出，补充内容可即时进入合并预览，保存/取消按钮状态正确，取消后未写入后端，控制台无错误。
- 本地 `effect-prompt-generation-worker` 镜像已按新代码重建并重新启动，RabbitMQ 消费队列已正常就绪，后续新批量生成任务会进入新版共用提示词编译流程。
- 本轮未调用 Ark 或 Seedance 付费接口，也未实现完整视频渲染任务；仅更新了第 4 节点的纯请求编译边界。

### 17.5 共用提示词单编辑框调整

- 当前状态：已完成。
- 用户界面不再分别展示“系统共用内容”“补充共用内容”和“最终共用提示词”，统一收敛为一个“共用提示词”编辑框，直接显示下游实际使用的完整内容。
- 内部仍保留系统禁用元素与用户补充来源，避免破坏上游维护关系；保存完整文本时，服务端校验并保留上游禁用元素，只有禁用元素之后的内容作为用户补充段保存。
- 保留 revision 并发校验、待校验生命周期、单条正文不回写和 Seedance 只追加一次的既有边界。本轮不修改 Prompt 生成、渲染参数或其他概览组件。
- 实际验证通过：Contracts 专项 13 项、API Service 专项 14 项、Web 页面与 API 专项 15 项；Contracts、API、Web 类型检查和生产构建均通过。真实页面确认仅存在 1 个编辑框，修改与取消状态正确，720px 窄屏无横向溢出且浏览器控制台无错误。

## 18. 2026-08-26 营销关系规划超时与重试治理

### 18.1 当前状态与业务边界

- 当前状态：已完成。
- 本轮只修复效果类第 3 节点“Prompt 生成”的异步执行可靠性和安全状态展示；直接上游、领域结果、`prompt-batch:{productId}` 提交边界及六类 Prompt 业务规则均保持不变。
- 已确认“营销关系规划”真实调用曾耗时约 161～212 秒，而当前 Ark 请求统一超时为 120 秒，慢响应会被误判为超时；Provider 内部重试与任务级重试叠加后进一步放大等待时间。
- 本轮不修改模型、输出 Token 上限、Prompt 模板、Prisma Schema、HTTP 路径，也不开发第 4～6 节点能力。

### 18.2 实施方案

- Prompt Worker 拆分超时：营销关系规划固定默认 300 秒，六类候选生成固定默认 120 秒；保留 `ARK_TIMEOUT_SECONDS` 作为候选超时的旧配置兼容。Provider 单次调用默认只尝试 1 次，业务级最多 3 次重试继续由 API Run 与 Outbox 统一维护。
- Worker 成功写回 `/fail` 后结束当前 RabbitMQ 消息，不再把同一消息立即重新入队；只有失败状态无法持久化时，消息层才执行一次保护性重投。
- Prompt Run 增加与信息提炼一致的过期租约恢复：未达到上限时原子恢复为 `QUEUED` 并重新激活 Outbox，达到上限时进入 `FAILED`；旧 attempt token 在新租约产生后不得继续写入。
- 公开任务状态增加当前尝试次数与最大次数，页面进度区和子工作流详情显示“第 N/3 次尝试”；上一次模型请求超时时展示安全化的自动重试提示，不暴露模型原始异常、Prompt 或内部标识。

### 18.3 测试与验收标准

- 覆盖 120～300 秒之间的策略响应、可重试超时、不可重试错误、三次上限、失败持久化异常、Worker 失联、租约恢复和旧 token 拒绝。
- 验证 API Run、Outbox 和 RabbitMQ 之间只有一套有效业务重试计数，任务失败时不覆盖上一份有效 Prompt 结果。
- 验证页面刷新后仍从后端恢复尝试次数、最近更新时间和重试提示，终态失败后允许用户重新批量生成。
- 完成后补充真实 pytest、NestJS、Vue、类型检查、构建、Worker 重建和运行任务验收结果，并将本节状态更新为“已完成”。

### 18.4 实施与验收结果

- Prompt Worker 已增加策略 300 秒、候选 120 秒的分阶段请求超时，并保留 `ARK_TIMEOUT_SECONDS` 作为候选生成兼容回退；Provider 默认最大尝试次数已从 3 次收敛为 1 次。重建后的容器已核对三项新环境变量分别为 `300`、`120` 和 `1`。
- Worker 成功持久化失败状态后会确认当前 RabbitMQ 消息，后续业务重试只由 API Run 与 Outbox 重新投递；只有失败状态无法持久化时才允许消息执行一次保护性重投。
- Prompt Repository 已增加 15 秒周期的过期租约恢复：未达到三次上限时原子恢复 Run 与 Outbox，达到上限时明确失败；所有阶段、心跳和结果写入仍需匹配当前有效 attempt token。
- 公开 Run 已增加 `attemptCount` 与 `maxAttempts`。页面进度区显示中文阶段名、下一次或当前尝试序号和安全化重试提示；子工作流当前节点详情使用 Run 心跳更新时间，避免长请求期间一直显示节点启动时间。
- 实际自动验证通过：Contracts 22 项；NestJS API 全量 230 项及生产构建；Prompt Web 全量 147 项及生产构建；Prompt Worker pytest 81 项、mypy 15 个源文件和本轮 Python 文件 Ruff 检查。API 与 Web TypeScript 类型检查通过。
- 部署前核对到旧任务 `bf5af701-fa4f-40a0-b73a-434f38f5c61f` 已在旧逻辑下完成三次 120 秒超时并进入 `FAILED`，因此重建 Worker 没有中断有效租约。新版 Worker 已成功启动并重新消费 `effect.prompt-generation.requested` 队列。
- 本轮未发起新的 Ark 或 Seedance 付费请求；运行级验收采用旧任务状态、容器配置、消费者启动日志和自动化超时/重试模拟完成。用户下一次点击“重新批量生成”时将使用新版 300 秒策略超时和统一任务重试逻辑。

## 19. 2026-08-27 营销关系规划输出收缩与截断治理

### 19.1 当前状态与根因

- 当前状态：已完成；真实规划节点验收通过，完整 50 条付费复跑留待下次明确授权。
- 本轮仍只修改效果类第 3 个业务节点“Prompt 生成”及其必要的公开任务状态，不修改直接上游、`prompt-batch:{productId}` 提交边界、Prisma Schema、HTTP 路径或第 4～6 节点。
- 真实任务 `f6f11f43-3bac-45e6-b19e-b7e303ad3153` 的三次营销关系规划分别耗时约 231 秒、215 秒和 235 秒；Ark 均返回 HTTP 200，但结构化 JSON 分别在第 477、448 和 437 行中途结束，最终错误为 `AI_RESPONSE_INVALID`。因此本次故障不是 300 秒超时，而是当前 8192 Token 输出预算下的结果截断。
- 旧 V7 契约要求一次返回场景、人物、卖点、证据、六类各五组长文本策略和完整关系束，最少包含 90 段策略描述；同一过大请求被任务层重试三次不会自然恢复。

### 19.2 实施方案

- 策略 Prompt 升级为 V8。模型只返回由 `bundleId`、单一 `fragmentType` 和 `factIds` 构成的精简营销关系骨架，每类 1～4 组、整批最多 24 组，不再返回事实原文、维度池、证据长文本或六类动作池。
- Worker 从已确认事实确定性构建场景、单一人物、卖点和安全证据，并复用六类职责安全基础库生成每类三个开场、动作、镜头、情绪和结束方案；现有正交扩展、蓝图编排、候选生成、覆盖门禁和定向补齐继续消费兼容的完整 `StrategyPlan`。
- Provider 在解析 JSON 前检查 Ark `status` 与 `incomplete_details`。输出达到上限时返回不可重试的 `AI_OUTPUT_TRUNCATED`，其他未完成响应返回 `AI_RESPONSE_INCOMPLETE`；仅完整响应中的偶发 JSON 异常允许整个任务额外尝试一次。
- 策略输出上限保持 8192 Token，默认超时从 300 秒收敛为 180 秒；候选生成保持 120 秒，Provider 单次调用保持一次。
- Run 终态失败时同步把当前 Stage 更新为 `FAILED`。页面只展示安全错误、关系束数量、Worker 补齐数量和事实覆盖数量，不暴露内部 JSON、模型输出或存储信息。

### 19.3 测试与验收标准

- Worker 覆盖精简契约、未知或冲突事实拒绝、缺失覆盖确定性补齐、完整 `StrategyPlan` 展开、Ark 未完成响应识别和安全日志。
- API 覆盖不同错误码的重试上限、Run 与 Stage 终态一致、Outbox 是否重新激活以及旧 attempt token 拒绝。
- Web 覆盖输出截断中文提示、无自动重试提示、终态失败节点状态和刷新恢复。
- 完成 Contracts、Worker、API、Web 类型检查、测试和生产构建后，重建 Prompt Worker，并执行一次已获授权的“广式腊肠”50 条真实 Ark 验收；记录真实耗时、Token、最终数量和六类配额，不记录模型正文。

### 19.4 实施与验收结果

- 策略模板已升级为 `effect-prompt-strategy-v8`，Ark 严格 Schema 只允许 `relationshipBundles`，每组只含 `bundleId`、`fragmentType` 和 `factIds`，整批最多 24 组。Worker 已实现关系束职责与来源过滤、必须事实补齐、场景/人物/卖点/安全证据和六类动作池的确定性展开，后续 `StrategyPlan`、蓝图编排和六类分支接口保持兼容。
- Provider 已在正文解析前检查 Ark `status` 与 `incomplete_details`；`max_output_tokens` 截断映射为不可重试的 `AI_OUTPUT_TRUNCATED`，其他未完成响应映射为 `AI_RESPONSE_INCOMPLETE`。完整响应的 JSON 异常最多进行一次额外业务尝试，网络、超时、429 和 5xx 仍遵守三次上限。安全日志仅记录状态、原因、Token、耗时和尝试次数。
- API 终态失败会同步关闭当前 Stage；公开 Run 与工作区状态增加安全错误码和尝试次数，页面展示第 N/3 次尝试、自动重排提示和刷新后的权威状态。策略节点详情只公开模型有效关系束数、Worker 补齐关系束数和计划事实数。
- 自动验证实际通过：Prompt Worker pytest 84 项、mypy 29 个源/测试文件和 Ruff；Contracts 23 项、类型检查与构建；NestJS API 232 项、类型检查与生产构建；Prompt Web 148 项、Vue 类型检查与生产构建。
- Prompt Worker 镜像已按 V8 重建。真实“广式腊肠”Run `f84a1a85-a4f8-44b7-b670-9cc62cc92ce6` 的营销关系规划在第 1 次尝试内完整完成：耗时 44.905 秒，输入 4247 Token、输出 2514 Token、总计 6761 Token，并立即进入片段蓝图编排和六类候选生成；未出现 JSON EOF、输出截断或规划重试。
- 该唯一获授权付费 Run 在后续候选阶段暴露了既有的模型 `usedFactIds` 改写问题，未产出最终 50 条。现已改为由 Worker 不可变蓝图统一回填候选事实绑定，模型无权改写内部 ID，并增加专项回归测试；为遵守“一次付费验收”授权，本轮未再次发起 Ark。Mock 全图与回归测试继续验证精确 50 条及六类配额，完整真实 50 条终态将在下一次明确授权后复验。

## 20. 2026-08-27 Prompt 子工作流 V9：六类 AI 营销规划

### 20.1 当前状态与实施边界

- 当前状态：已完成；六类真实规划验收通过，完整 50 条真实终态受既有候选 JSON 偶发异常阻断，未擅自追加第二次付费批次。
- 本轮只改造效果类第 3 个业务节点“Prompt 生成”的内部营销规划阶段，以及必要的共享契约、内部 Worker 检查点、公开安全状态和测试；不修改 `prompt-batch:{productId}` 提交边界、Prisma Schema、现有公开 HTTP 路径或效果类第 4～6 节点。
- V8 解决了单次规划输出截断，但把场景、人物、动作、镜头和情绪较多地交给固定规则展开，存在创意上限和母版内部关系被再次拆散的风险。V9 改为“全局事实分配 → 六类 AI 营销规划并行分支 → Worker 合并校验”，让模型在小输出范围内重新负责创意母版。

### 20.2 已确认实现口径

- Worker 确定性生成六类 `FragmentFactAllocation`，每类母版目标为 `min(4, max(1, ceil(fragmentCount / 3)))`，并为每个强制事实确定一个职责兼容的主要承载类型。
- 六类规划分支分别返回事实绑定、创意切入、场景、人物、开场、连续动作、镜头、情绪和结束状态；每次只处理一种片段，严格覆盖本类强制事实，不输出最终 Prompt 或渲染参数。
- 六个规划分支并行执行，使用独立配置且默认跟随候选 Turbo。可重试错误只重新执行失败分支，成功分支以 `sourceFingerprint + allocationHash + Prompt 版本` 检查点恢复；达到上限后整批失败，不使用规则创意替代。
- 蓝图编排必须整体继承同一营销母版的场景、人物、动作、镜头和结束状态，只允许有限的构图、道具和机位细节变化，不再从独立词池正交乱配。
- 新 Run 使用 `V9_SIX_BRANCH_STRATEGY` 拓扑；历史缺少版本的 Run 按 `V8_SINGLE_STRATEGY` 展示原节点和原结果，不伪造六类规划记录。

### 20.3 测试与验收

- 覆盖事实分配、六类严格 Schema、安全证据冲突、并行分支、失败分支检查点恢复、哈希失效、旧 token 拒绝、合并覆盖和母版整体继承。
- 回归精确总数、六类配额、三轮补齐、覆盖门禁、单条重新生成、共用提示词、公开信息脱敏和历史 V8 拓扑。
- 自动测试、类型检查和生产构建全部通过后，执行一次已授权的“广式腊肠”真实 50 条 Ark 验收，记录六类规划耗时、Token、最终数量和安全覆盖，不记录模型正文。

### 20.4 实施与验收结果

- Contracts 已增加 V8/V9 双拓扑和 `graphVersion`；新任务写入 `V9_SIX_BRANCH_STRATEGY`，历史缺少版本的任务继续投影为 V8。API、Web 和 Worker 已接入全局事实分配、六类规划路由、六个并行规划节点、合并校验及版本一致的安全详情。
- 对部署切换期间“旧 API 创建快照、新 Worker 实际执行 V9”的特殊任务，API 会依据已经持久化的真实 V9 阶段识别拓扑；只有既无显式版本、也无 V9 阶段的历史任务才按 V8 展示，避免真实执行了六分支却仍显示旧“营销关系规划”。
- Worker 已新增 `FragmentFactAllocation`、六类严格 `FragmentMarketingPlan`、检查点哈希校验和 `six-ai-branch-v2` 蓝图编排。蓝图的场景、人物、动作、镜头、情绪与结束状态整体来自同一 AI 母版，Worker 只增加有限可执行细节，不再跨母版拆池乱配。
- 配置已落地：六类规划默认跟随候选 Turbo，单支超时 120 秒、输出上限 3072 Token、Provider 单次调用 1 次；默认图并发为 6。失败分支可复用同 Run 内其余五个成功检查点，检查点仅保存验证后的结构化计划和哈希，不保存原始响应。
- 自动验证实际通过：Prompt Worker pytest 87 项、mypy 与 Ruff；Contracts 23 项及构建；NestJS API 31 个测试文件共 242 项、类型检查和生产构建；Prompt Web 27 个测试文件共 148 项、类型检查和生产构建。Mock 全图保持精确 `10/8/12/10/6/4` 共 50 条。
- 根目录 ESLint 本轮执行后仅剩 2 个与本任务无关的既有错误，均位于用户保留的信息提炼测试改动中的未使用 `_resolution` 变量；Prompt V9 相关文件未产生 ESLint 错误，本轮未越界修改信息提炼模块。
- 真实“广式腊肠”Run `4f216b18-e00b-4d5c-a9fc-cbfff90ab496` 完成 22 个强制事实的全局分配，得到 19 个母版目标；六支全部一次成功并完成合并校验，实际母版数分别为 `4/3/4/4/2/2`，每支强制事实覆盖均为完整覆盖，全局合并得到 19 个关系束和 35 个已规划事实引用。
- 六支真实耗时为 18.720～38.351 秒，均小于 120 秒；单支输出 851～1718 Token，均小于 3072。六支合计输入 21,251 Token、输出 7,782 Token，约 38.5 秒完成并进入片段蓝图编排，未发生输出截断或规划重试。
- 该唯一获授权付费批次随后在既有候选补齐阶段遇到一次 `status=completed` 但 JSON 末尾缺失的偶发 `AI_RESPONSE_INVALID`，因此未形成真实 50 条终态。验收同时发现“部署握手失败曾占用一次任务尝试，导致格式异常没有自己的额外重试机会”，现已改为按错误类型维护一次内部重试账本，并把并行生成失败精确归属到实际片段节点；未为本次验收绕过门禁、直接改库复活任务或自动发起第二个付费批次。

## 21. 2026-08-27 Prompt 子工作流 V10：六类坐标规划与组合级蓝图生成

### 21.1 当前状态与目标

- 当前状态：已完成。
- V9 将营销事实组合与场景、人物、动作、镜头、情绪合并为同一个创意母版，后续再由程序添加有限变体；这使“事实为什么组合”与“片段具体如何拍摄”职责重叠，也容易产生文字变化但画面同质的问题。
- V10 保留六类并行营销组合节点，但其输出收缩为事实关系；新增六类产品专属坐标规划和组合级蓝图生成。每张合格蓝图严格对应一条 Prompt，程序只分配配额、校验和筛选，不拆解重组模型坐标。
- 本次只改造效果类第 3 个业务节点及必要契约、Worker、持久化、节点详情和测试；不修改最终结果 Schema V5、公开工作区路径、WorkingArtifact 提交边界、视频渲染、混剪或成片节点。

### 21.2 固定拓扑与模型分层

```text
输入快照 → 提炼信息映射 → 共用提示词编译 → 全局事实分配
  → 六类营销组合条件路由 → 营销组合合并校验
  → 六类坐标条件路由 → 坐标计划合并校验
  → 蓝图配额分配 → 六类蓝图条件路由 → 全批次蓝图正交校验
  → 六类 Prompt 条件路由
  → 结构、语义、视觉、覆盖和质量门禁
  → 蓝图定向补齐或结果保存
```

- 新图版本为 `V10_RELATION_COORDINATE_BLUEPRINT`；V8/V9 节点与详情语义冻结，历史 Run 不迁移、不伪造 V10 产物。
- 六类营销组合每类只输出 `bundleId、fragmentType、primaryFactId、factIds、creativeIntent`。组合数量继续采用 `min(4, max(1, ceil(fragmentCount / 3)))`。
- 六个坐标分支分别读取本类型全部营销组合和产品事实，各生成一份 `FragmentDimensionCoordinatePlan`。每份计划包含叙事、场景、人物、卖点、镜头和情绪六轴的多个产品专属候选值、适用组合、来源事实与规范签名，不允许固定六元组或通用模板回退。
- 每类目标数按等权最大余数分配到本类营销组合；余数优先覆盖必需事实、核心卖点，再按稳定组合顺序分配。蓝图任务按片段类型同质分片，每片最多 8 张，通过 LangGraph `Send` 并行执行。
- 营销组合、坐标和蓝图默认使用 Doubao Seed 2.0 Lite；最终 Prompt 使用 Doubao Seed 2.1 Turbo。新增 `ARK_PROMPT_BLUEPRINT_MODEL` 作为可选覆盖，默认回退策略模型。

### 21.3 蓝图、差异择优与数量优先门禁

- 蓝图模型只能从所属类型坐标计划中选择与营销组合兼容的完整六维坐标，并生成首帧、单一动作弧、结束状态和真实使用事实；不得新增坐标、事实、片段类型、时长或配额。
- 程序使用坐标的规范文本签名而不是类型局部 ID 做全批次比较；片段类型不计入六维距离。默认 50 张仍会比较全部 1225 对，但“至少三个维度不同”从逐条硬淘汰条件调整为择优排序和公开告警：优先选择最小距离更大的蓝图，普通相似不得直接造成数量缺口。
- 全局选择顺序固定为：六类目标数量与营销组合配额、必需事实覆盖、与已选集合的最小距离最大、稳定任务顺序。只有正文完全重复、事实或安全违规、片段职责冲突和真正不可执行的候选继续硬剔除；六维坐标相同但首帧、动作弧或结束状态不同的蓝图允许保留。
- Prompt 语义与画面相似度按完整批次的“违规对数 ÷ 全部 Prompt 对数”计算，以用户设置的上限为门禁，不再因为发现任意一对相似就删除其中一条。系统先得到用户要求的总数和六类配额，再用可用候选择优降低相似率；仍超限时保留精确数量并标记 `NEEDS_REVIEW`，而不是保存短批次。
- 轻微正文长度、光线/节奏或镜头描述不足降为可见质量告警，不减少数量；事实越界、完全重复、无可见动作、多镜头时间轴、证据伪造和片段职责冲突仍是硬门禁。蓝图或硬门禁造成真实缺口后，按 `ceil(缺口 × 1.25)` 回到原类型、原营销组合补齐蓝图，最多三轮，再由新蓝图生成 Prompt。
- 默认 50 条需要营销组合 6 次、坐标 6 次、蓝图 9 个分片、Prompt 9 个分片，共 30 次模型调用；最大并发保持 6，规划阶段均使用低成本模型。

### 21.4 数据、恢复与公开详情

- 新增严格内部契约 `FragmentRelationshipPlan`、`FragmentDimensionCoordinatePlan`、`BlueprintBundleQuota`、`BlueprintTask`、`GeneratedBlueprint` 和 `BlueprintShardPlan`。
- `EffectPromptShardOutput` 增加 `phase=BLUEPRINT|PROMPT`，唯一键调整为 `projectId + runId + phase + round + shardIndex`；历史数据回填为 `PROMPT`。营销组合与坐标使用 Stage checkpoint，蓝图与 Prompt 使用独立分片恢复。
- 公开节点详情增加营销组合、坐标计划、蓝图和全局正交内容块，每个内容型节点最多展示三条真实样例；不展示模型、系统 Prompt、Token、原始响应、内部 ID 或存储位置。
- 单条重新生成优先复用有效坐标检查点并只路由目标类型和目标组合；旧结果通过 `insightBindings` 匹配组合，新结果通过持久化蓝图分片精确恢复。

### 21.5 测试与验收口径

- Contracts：V10 节点、边、详情块和 V8/V9 冻结快照；六份坐标计划、蓝图一对一 Prompt 契约。
- Worker：六类营销事实关系、六类坐标并行、组合配额、9 个蓝图分片、全局 1225 对正交检查、定向补齐、单条重生成和四层断点恢复。
- API/Prisma：分片 phase 迁移、项目隔离、幂等、过期 attempt、V8/V9/V10 版本推断和公开详情脱敏。
- Web：四组六分支拓扑、真实营销组合/坐标/蓝图/冲突详情、刷新恢复、切换防串写、窄屏和键盘交互。
- 使用食品、日用品和设备类 Mock 样本完成 50 条 `10/8/12/10/6/4` 验收；执行 Contracts、NestJS、Vue、Worker pytest/mypy/Ruff、Prisma、Compose 和镜像构建。未经新的明确授权不调用真实 Ark 或 Seedance。

### 21.6 实施结果

- 共享契约已冻结为 `V10_RELATION_COORDINATE_BLUEPRINT`，V8/V9 拓扑和详情继续按历史版本只读。V10 新增六类营销组合、六类坐标计划、蓝图配额、六类蓝图分支和全批次正交节点；Web 直接读取共享图契约，不维护第二套节点常量。
- Worker 已落地三层严格结构：`FragmentRelationshipPlan` 只保存事实关系；每个片段类型只生成一份 `FragmentDimensionCoordinatePlan`；`GeneratedBlueprint` 按所属组合选择完整六维并生成首帧、动作弧和结束状态。一张合格蓝图严格物化为一条 Prompt，六维使用规范文本签名做全批次比较，类型本身不计入差异。
- 默认 50 条 Mock 批次实测调用结构为营销组合 6 次、坐标规划 6 次、蓝图分片 9 次、Prompt 分片 9 次，共 30 次；分片最大并发 6。Mock 不产生真实 Token，实际端到端从 claim 到完成约 4.3 秒，因此本节不伪造生产 Ark Token 或耗时数据，也未调用真实 Ark/Seedance。
- Prisma 迁移 `20260827150000_effect_prompt_v10_shard_phase` 已为蓝图/Prompt 分片增加 `phase`，历史记录回填为 `PROMPT`，唯一键包含 `phase`。迁移状态、Schema 校验和 Compose 配置均通过；Worker 镜像已按最终代码构建成功。
- API 最终质量复算与 Worker 门禁已统一：`调整、尝试、停下、停住、移开` 等真实动作按可见动作处理；保留绑定与推断绑定合并后严格限制为最多 3 项，避免 Worker 合格而服务端误判 `NO_VISIBLE_ACTION/FACT_OVERLOAD`。Mock 蓝图在同一营销组合内轮动覆盖尚未表达的事实，且单张仍限制为主要事实加最多两个辅助事实。
- 自动验证实际通过：Contracts 3 个文件 24 项；NestJS API 31 个文件 276 项；Prompt Web 28 个文件 157 项；Prompt Worker pytest 95 项、mypy 17 个源文件、Ruff 全目录检查。根目录 TypeScript/Vue 类型检查与生产构建通过，Web 构建为 1876 个模块，API 构建通过。
- 浏览器使用显式 Mock 完成当前产品 50 条真实持久化回归：六类分布为 `10/8/12/10/6/4`，全部 5 秒，语义重复率 0%、视觉重合率 0%、执行无效 0，最终结果保存节点为 `PASS`，卖点覆盖 3/3。工作流弹窗能够展示 V10 真实营销组合、坐标/蓝图阶段和结果保存详情；历史 V9 Run 仍按旧拓扑展示。
- 运行验收暴露并修复了两个仅在真实本地进程中可见的问题：旧 API 进程尚未加载 V10 DTO，以及旧 Docker Worker 不认识 V10 Claim。验收时已停止过期 Worker、重启最新 API，并使用最新显式 Mock Worker完成闭环；这属于部署滚动重启要求，不通过兼容性降级掩盖。

### 21.7 失败批次状态收口与 Prompt 临时预览

- 当前状态：已完成。
- 根因确认：候选补齐阶段的单个 Ark 分片被 HTTP 400 拒绝后，整批按精确数量规则不能保存；但首轮及其他成功分片已经持久化了候选。旧结果接口只读取完整 `EffectPromptResult`，因此页面下方错误显示“尚未生成 Prompt”。并行图中最后一次心跳还可能覆盖真实失败节点，造成一个分支失败、多个兄弟分支长期显示执行中的假象。
- API 终态失败现在把 `currentNode` 固定为 Worker 上报的真实失败节点；同一 Run 内仍为 `RUNNING` 的兄弟 Stage 收口为 `SKIPPED`，仍为 `RUNNING` 的分片收口为 `BATCH_ABORTED`。公开投影也会为历史失败 Run 优先采用已持久化的失败 Stage，并把被中断兄弟分支显示为“任务已停止，该分支未完成”。
- 当当前产品没有已保存完整结果时，结果接口会从最新失败 Run 的成功 Prompt 分片提取 `executionInvalidReasons=[]` 的候选，重新校验结构并返回只读临时预览。预览不创建 `EffectPromptResult`、不更新 `WorkflowNodeState` 或 `prompt-batch:{productId}`，也不改变精确数量门禁。
- 页面会明确标注“临时预览”，仅允许搜索、分页、查看与复制；人工新增、修改、删除、单条重生成、共用提示词编辑、批量导出和完成校验全部禁用。重新批量生成成功后，页面恢复正常可编辑结果。
- 自动验证实际通过：Contracts 3 个文件 24 项、NestJS API 31 个文件 273 项、Prompt Web 28 个文件 155 项；Contracts、API、Web 类型检查和生产构建通过。V10 分片 phase 迁移同时修正了 PostgreSQL 63 字节标识符截断造成的索引重名，迁移状态已恢复为最新。
- 使用真实失败 Run `83c89ec2-66f8-4904-a180-d2f35c78f831` 完成运行验收：公开状态只保留 `GENERATE_PRODUCT_DISPLAY` 一个失败节点，五个被中断兄弟分支均显示已跳过，接口恢复出 23 条临时 Prompt。浏览器第一页展示 10 张卡片与 10 个复制按钮，修改、删除、单条重生成、批量导出均不出现，完成校验保持禁用；页面无横向溢出且控制台无错误。

### 21.8 数量优先门禁整改

- 用户确认门禁第一目标是满足设置的总数量与六类配额，不再为了追求逐对零相似保存短批次。蓝图六维距离不足三项改为择优排序和公开告警；同坐标但首帧、动作弧或结束状态不同的蓝图允许保留。
- Prompt 选择先补足六类数量和必须事实，再按新增语义冲突数、视觉冲突数和六维距离择优。语义、视觉相似率按完整批次对数计算；阈值内相似正常保留，超限时保留精确数量并标记 `NEEDS_REVIEW`。
- 完全重复正文、事实或安全违规、不可拍证据、完整时间轴、动作/运镜冲突和片段职责冲突仍是硬门禁。正文长度、轻微缺少镜头或光线描述、抽象人物表达和重复负向尾段改为非阻塞质量告警。
- Worker 与 NestJS 使用同一软硬分类；公开完成校验接口返回非阻塞 `warnings`，页面不再用“任意两条至少三维不同”作为失败提示。坐标计划只需提供真实六维候选并覆盖所属营销组合，不再因理论正交容量不足阻塞模型结果。
- 本轮已在整改过程中完成 Worker、API、Contracts 和 Web 的定向/全量自动验证；用户随后明确取消新的付费端到端测试，因此没有再次调用 Ark 或 Seedance，也没有为旧短批次伪造通过结果。
