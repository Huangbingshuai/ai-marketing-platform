# 效果类工作流 Step 01「资料包导入」实施方案

> 状态：历史方案，节点级“保存到项目资产库”能力已废弃。当前实现与验收以 [工作流草稿与工作副本自动维护方案](./工作流草稿与工作副本自动维护方案.md) 为准；本文后续关于 publish、手动入库、正式资产覆盖和版本的章节仅保留为历史决策记录，不代表现行行为。

## 1. 范围与边界

本次只实现效果类工作流第 01 步“资料包导入”，包括单产品导入、多品类批量导入、全局视频配置、单品配置覆盖、草稿恢复、项目隔离、资料校验、正式资产入库和进入下一节点的门禁。

本次明确不实现：AI 信息提炼业务、Prompt 生成、Seedance 调用、渲染、混剪、时间轴精修、成片导出、TOS Adapter、通用 WorkflowRun 历史版本和用户权限体系。校验通过后只进入“AI 信息提炼暂未开发”占位，不创建 AI 或异步任务。

## 2. 原型基准与已确认偏离

必须参考的冻结原型：

- `D:\1Code\ai-marketing-platform\references\prototypes\effect\effect-workflow.html`
  - 参考工作流画布、Step 01 标题区、单产品上传区、批量商品卡片、配置卡、文件卡、状态反馈、底部操作区。
- `D:\1Code\ai-marketing-platform\references\prototypes\integrated\system-integrated-demo.html`
  - 参考 68px 顶部导航、64px 项目上下文栏、项目与资产入口、页面容器和蓝白视觉。

视觉基准：主蓝 `#2563eb/#2766ed`，浅蓝背景 `#f7faff/#f8fbff/#eef3ff`，边框 `#dbe4f6`，白色卡片，正文 `#17233a`，主要圆角 16/20/26px，工作区 28px 内边距，桌面主容器左右留白 28px。

已确认的需求优先偏离（2026-08-20 用户视觉复核后更新）：

- 单产品桌面端固定左侧资料导入、右侧全局配置，1:1、18px 间距。
- 用户以三张原型截图明确要求正式 UI 完全贴合冻结原型；批量模式恢复原型的“两列商品卡片 + 下方整行全局配置”，此项为最新指令，覆盖此前批量模式也固定右栏的决定。
- 所有下拉框统一向上展开，并支持搜索和自定义；原型中向下展开的下拉不保留。
- 新增资料类型、CSV/XLSX、批量工具、配置覆盖和服务端状态，但延续原型的卡片、按钮、密度和反馈样式。产品名称由用户在每个资料包上传前填写；品类由下一节点 AI 提炼，SKU 由系统内部生成且不向用户暴露。
- 删除“模拟解析”“仅本地演示”等文案；所有可见操作接真实接口。

视觉验收不以“同类蓝白风格”为通过标准，而以冻结原型和用户提供截图逐区对比：小号导入模式 segmented、单品大拖拽卡与等高配置卡、全宽链接卡、批量两列商品卡、珊瑚编号/状态标签、暖浅边框、文件白色行、下方全宽配置卡和正式入库动作条都必须保持原型的信息密度、尺寸、间距、圆角和层级。

## 3. 已确认产品规则

- 单产品与批量分别持久化产品数据、全局配置、校验状态和 revision，切换不删除、不覆盖另一模式数据。
- SKU 由后端自动生成并保证当前模式草稿内唯一，前端表单和 CSV/XLSX 清单均不要求用户输入。
- 最低通过条件：每个资料包必须有产品名称和至少一张 READY 商品图片，全局配置及覆盖配置合法；电商链接如填写必须合法。品类不属于本节点校验项。
- 产品资料只提供两类新增上传：商品图片（主图、细节图、场景图）和产品文档（产品卖点、受众画像、合规禁忌词、投放渠道）。不再提供“品牌规范”和“参考视频”上传入口；旧草稿数据仅保留读取和删除兼容。
- CSV/XLSX 必须先由后端预览；用户确认后所有行写入草稿，错误行同样创建并可在页面修复。
- 服务端已保留文件内容时可直接失败重试；网络中断或内容未保存时必须重新选择文件。
- 当前模式的 READY 文件和每个产品的有效配置均可“保存到项目资产库”；本节点不向用户创建或展示版本。每次新的主动保存都物理删除上一批效果类导入资产，并从当前草稿重新创建全部正式资产和 MinIO 正式对象；旧 MinIO 正式对象在数据库重建成功后删除。未由用户上传的内部产品草稿字段不创建独立资产。
- “保存到项目资产库”不要求用户预先点击独立校验按钮。保存动作先提交待保存编辑，再对当前 revision 执行权威校验；校验通过后继续入库，失败则停留当前节点并展示问题。独立校验按钮和下一节点门禁继续保留。
- 每个 READY 图片或文档独立创建 `SOURCE_MATERIAL` 文件资产；每个产品另建一项 `VIDEO_CONFIG`。正式资产必须使用用户填写的产品名称，不再使用“商品资料包 01”或“未命名产品”回退。产品文档只有在用户实际上传后才以文件资产入库，不根据空的内部产品字段生成“产品资料”卡片。
- 入库成功后留在当前工作流，刷新草稿和项目上下文统计；用户之后打开资产库时读取最新列表，不自动打开资产抽屉。
- `SOURCE_MATERIAL` 文件根据服务端 `previewKind` 展示：图片使用项目隔离的 content API 生成真实缩略图，文档继续显示文档封面，加载失败显示占位。

## 4. 前端结构与状态

现有 `ProjectManagementPage.vue` 继续负责系统外壳、项目上下文和资产抽屉，效果类页面挂入其 `<main>`，不引入 iframe、Vue Router、Pinia 或新的 UI 库。

入库动作使用 `idle → validating → publishing → idle` 独立状态覆盖完整操作，期间禁用重复点击。前端依次复用 validate、publish 两个现有接口；发布成功后并行刷新当前草稿和项目上下文。项目统计刷新失败不得回滚已经成功的正式资产，重新打开资产库仍会通过列表接口取得权威数据。

建议组件树：

```text
EffectImportNodePage
├─ EffectWorkflowCanvas
├─ EffectImportWorkspace
│  ├─ ImportNodeHeader
│  ├─ ImportModeSegment
│  ├─ ImportSourceColumn
│  │  ├─ ProductIdentityForm
│  │  ├─ MaterialUploader / MaterialList
│  │  ├─ CommerceLinkEditor
│  │  ├─ BatchToolbar / BatchValidationSummary
│  │  └─ EffectProductCardList
│  ├─ GlobalVideoConfigPanel
│  └─ ImportNodeFooter
├─ BatchManifestImportDialog
├─ ProductConfigOverrideDialog
└─ EffectUpwardCreatableSelect
```

页面状态至少区分：无项目、加载、空、成功、失败；保存状态区分 clean、dirty、saving、saved、saveError、conflict；文件状态区分 queued、uploading、ready、failed、deleting；校验状态区分 stale、validating、invalid、valid。

文本和配置使用 600ms 防抖保存，失焦立即 flush。产品字段和全局配置分别使用带递增版本号的 dirty snapshot；旧请求响应只能确认其发送时的版本，不得覆盖飞行期间产生的新编辑。所有写请求进入按项目隔离的单飞队列；模式或项目切换前必须先用旧项目上下文 flush，失败时阻止切换并保留可重试快照；成功后才 abort 请求、增加 generation、清空旧数据并加载新项目草稿，禁止迟到响应污染当前项目。批量列表和完整草稿中的同一产品必须按 productId 同步回写。

全局配置保存最终语义值，不保存下拉控件的 UI 状态：

- 视频时长：原型数字输入，允许 1–300 秒。
- 画幅比例：9:16、16:9、1:1、4:3、3:4、2:3、21:9、自定义。
- 风格基调：严格使用冻结原型的 16 个预置项（烟火食欲感、高端电影感、自然电影感、户外电影感、清透冰感、居家纪实、香槟金电影感、专业测评感、温馨生活感、国潮新中式、清新田园、复古胶片、赛博霓虹、极简商务、治愈暖光、夜景氛围），默认“烟火食欲感”并支持自定义。
- 投放渠道：预置选项并支持自定义。
- 禁用元素：原型单行输入，服务端按顿号、逗号或换行拆分保存，最多 50 项。
- 本节点不展示分辨率、帧率、字幕策略、口播策略或 BGM 策略；历史草稿中的对应内部值仅保留兼容，不构成新增 UI。

产品配置覆盖使用 `Partial<EffectVideoConfig>`；缺失字段继承全局值，返回时同时提供 override 和服务端计算的 effectiveConfig。

## 5. 共享契约与 Prisma 模型

新增 `packages/contracts/src/effect-import.ts`，统一导出模式、状态、视频配置、产品、资料、校验 issue、清单预览/提交、批量操作、正式入库和推进契约。所有 JSON API 继续使用 `ApiResponse<T>`。

Prisma 新增：

- `EffectImportWorkspace`：`projectId` 唯一，保存当前模式和 workspace revision。
- `EffectImportDraft`：`@@unique([projectId, mode])`，保存全局配置、revision、validatedRevision、校验结果、状态、完成时间和最近入库摘要。
- `EffectImportProduct`：包含 projectId、draftId、名称、品类、SKU、normalizedSku、链接、configOverride、排序和清单来源；名称由用户填写并在上传前由 API 权威校验，品类预留给下一节点 AI 提炼回写。
- `EffectImportMaterial`：包含 projectId、productId、资料类型、MISSING/UPLOADING/READY/FAILED、期望文件名、实际文件信息、storageKey、安全错误和重试次数。
- `EffectManifestImport`：保存 PREVIEW/COMMITTED/CANCELLED/EXPIRED、规范化预览行、issues、幂等键和过期时间。
- `EffectManifestStagedFile`：保存预览阶段上传的配套文件与匹配结果；提交后转为草稿资料，取消/过期后清理。
- `EffectImportPublishOperation`：以 projectId、客户端 idempotencyKey 唯一记录一次主动入库操作、草稿 revision、完整快照、持有令牌、执行结果和失败状态；部分成功后可按同一操作安全续跑。
- `EffectImportPublishFileHold`：发布运行或恢复期间持有快照引用的 READY 存储对象，避免实时草稿删除/重传破坏尚未完成的入库。
- `StorageCleanupTask`：持久记录工作流删除、重传和清单清理后待删除的存储键；立即执行失败时由后续初始化/请求重试，禁止吞掉异常后永久泄漏。

Draft、Product、Material、Manifest 关系使用包含 `projectId` 的复合外键。所有 Repository 方法必须把 `projectId` 放入查询、更新和删除条件。草稿允许保存空的用户可编辑字段，以便错误行可恢复；SKU 是系统内部标识，创建产品时自动生成。

每次草稿内容修改都原子增加 revision，并清除 validatedRevision。写请求使用 `If-Match` 或 body expectedRevision；Controller 在校验后统一解析二者，至少存在一项，不一致返回 409。只有 `validatedRevision === revision` 且校验无 error 时允许 advance；已推进草稿再次修改时必须把 Step 01 恢复为 CURRENT、Step 02 重新锁定。

## 6. REST API

基础路径：`/api/projects/:projectId/workflows/effect/source-import`

- `GET /`：workspace、双模式草稿摘要、默认配置和节点状态。
- `PATCH /mode`：无损切换当前模式。
- `GET /drafts/:mode`：加载完整模式草稿。
- `PUT /drafts/:mode`：创建/更新该模式全局配置。
- `GET /drafts/:mode/products`：keyword、page、pageSize 查询；当前界面按电商链接或资料文件名搜索，category 参数仅保留历史接口兼容。
- `POST /drafts/:mode/products`：新增产品。
- `PATCH /drafts/:mode/products/:productId`：更新字段或配置覆盖。
- `DELETE /drafts/:mode/products/:productId`：删除产品和草稿资料。
- `POST /drafts/:mode/products/batch-delete`：原子批量删除。
- `POST /drafts/:mode/products/batch-retry`：对保留 storageKey 的 FAILED/RETRYABLE 资料重新执行真实校验/处理；无服务端内容的资料返回 REQUIRES_NEW_FILE。
- `POST /drafts/:mode/products/:productId/validate-link`：只校验、规范化 HTTP/HTTPS 链接，不抓取外部页面。
- `POST /drafts/:mode/products/:productId/materials`：multipart 新增资料。
- `PUT /drafts/:mode/products/:productId/materials/:materialId/content`：重传、补传或带新文件重试。
- `DELETE /drafts/:mode/products/:productId/materials/:materialId`：删除资料。
- `GET /drafts/:mode/products/:productId/materials/:materialId/content`：原始流，视频支持 Range。
- `POST /drafts/BATCH/manifest-imports/preview`：multipart 清单和可选 files[]，返回逐行和文件匹配问题。
- `POST /drafts/BATCH/manifest-imports/:importId/commit`：幂等确认写入。
- `DELETE /drafts/BATCH/manifest-imports/:importId`：取消并清理暂存文件。
- `GET /manifest-template?format=csv|xlsx`：下载正式模板。
- `POST /drafts/:mode/validate`：权威校验并保存 validatedRevision。
- `POST /drafts/:mode/publish`：携带客户端 idempotencyKey，将当前模式完整资料包正式入库；同 key 为同一次请求恢复，新 key 表示用户再次主动入库。
- `POST /drafts/:mode/advance`：标记 Step 01 完成并返回 `AI_INFO_EXTRACTION`。

所有写接口返回最新 revision 和统一 JSON 响应，不使用 204。实体不存在或不属于当前项目时使用相同 404 行为，禁止跨项目枚举。

## 7. 文件、清单与正式资产

文件持久化复用 `StoragePort`；效果模块不依赖 `LocalStorageAdapter`。上传/重传先保存新对象，再事务更新数据库；数据库失败时删除新对象，旧对象仅在事务成功后删除。草稿资料重传后只保留新的 staging 对象；已经入库的资料在下一次保存成功后只保留新的正式对象。删除失败进入 `StorageCleanupTask`，不得因为清理异常回滚已经成功的数据库切换。服务端内容已保存但后续处理失败时保留 FAILED/RETRYABLE 记录供真实重试；内容未保存时记录 REQUIRES_NEW_FILE。底层路径和存储异常不得返回客户端。

白名单与默认限制：

- 批量草稿最多 100 个产品。
- CSV/XLSX 最大 10 MiB、100 行，只读取第一张非空工作表，不执行公式。
- 商品图片 50 MiB；产品文档 100 MiB。
- Multer 在落盘前限制清单 10 MiB、配套文件数量和配套单文件 100 MiB；业务层继续按图片 50 MiB、文档 100 MiB 做权威限制。
- 扩展名、MIME 和必要文件签名联合校验；零字节拒绝。
- 清单文件列：电商链接、商品图片、产品文档；每一行代表一个独立产品资料包，多文件名使用 `|` 或换行分隔。产品名称、品类由下一节点 AI 提炼，SKU 在提交时由系统生成。
- 文件名按安全 basename、Unicode NFKC、大小写不敏感精确匹配；找不到或重复匹配均产生结构化 issue。
- 预览 24 小时过期；PREVIEW 到 COMMITTED/CANCELLED/EXPIRED 使用包含 projectId、draftId、当前状态的条件迁移。取消、过期及提交完成后只清理经二次确认仍未 transferred 的对象，提交时同步清理未匹配或歧义的配套文件。

后端新增唯一生产依赖 `exceljs`，只安装到 `apps/api`。它解决 CSV/XLSX 解析问题；现有依赖无法替代，自写 XLSX ZIP/XML 解析不可维护。前端包体不受影响。

正式入库由效果 Service 调用公共 `AssetService`：文件型工作流产物每次都从 staging storage 写入新的正式对象，有效视频配置每次都重新创建无文件资产；产品文档必须来自真实 READY 文件。稳定产物幂等键只用于定位并删除上一批资产，不用于复用当前资产。每个产物在单个数据库事务中删除旧 `AssetOperationReceipt`、旧兼容 `AssetVersion` 和旧 `Asset`，随后创建新的 Asset 与单条 `version=1` 兼容快照；数据库事务失败时删除刚写入的新正式对象，旧数据库资产保持不变。数据库重建成功后，旧正式 `storageKey` 交给 `StorageCleanupTask` 删除。底层兼容快照不生成时间线，也不暴露给本节点用户。同一客户端 idempotencyKey 的网络重试或失败恢复继续复用新回执，不重复重建；新的主动保存键才触发整批重建。claim 在事务内锁定并权威校验 draft revision/validatedRevision 后保存完整入库快照和文件 hold，后续从快照安全恢复；资产名称和标签在发布前按平台长度约束安全映射。

单产品包含三张 READY 图片时，每次入库均得到 4 项正式资产：三项图片文件和一项视频配置。资产库“原始资料”显示前三项，“视频配置”显示后一项；再次主动保存时旧的 4 项资产被物理删除并创建新的 4 项，三张图片均生成新的 MinIO 正式对象，上一批三个正式对象随后删除，因此最终资产数和正式对象数保持不变但资产 ID、正式 `storageKey` 均更新。视频配置资产只展示本节点当前允许用户编辑的视频时长、画幅比例、风格基调、投放渠道和禁用元素；分辨率、帧率、字幕、口播和 BGM 等内部兼容默认值不得作为用户配置展示。本节点资产卡、详情和预览均不显示版本号、版本时间线或“生成新版本”入口。

## 8. 开发步骤

1. 连续检查工作区状态并记录现有 diff；如仍有持续写入则暂停。
2. 先完成共享契约和契约测试。
3. 后端实现 Prisma 迁移、EffectImportModule、Repository、Service、Controller、DTO、清单解析、存储、校验、入库和推进。
4. 前端实现工作流画布、固定左右布局、单/批量页面、API、保存队列、上传和范围外占位。
5. 主代理整合并审查项目隔离、平台目录边界、响应契约和原型还原度。
6. 只格式化本任务文件，不全仓重写，不覆盖、回退或提交其他任务代码；在用户于 2026-08-21 明确授权后，仅提交本节点相关文件。

## 9. 测试与浏览器验收

自动测试：

- Prisma validate/generate/迁移验证。
- Contracts/API/Web 类型检查。
- Repository：projectId 条件、复合关系、revision、分页筛选、批量原子性。
- Service：双模式恢复、A/B 项目隔离、系统 SKU 生成、URL/配置、校验失效、推进门禁、上传补偿、重传、两类失败重试、正式入库整批重建和旧对象清理。
- 清单：CSV BOM/引号/换行、XLSX、缺表头、损坏、公式、超限、缺字段、坏链接、文件缺失/重复匹配、取消、幂等提交。
- Web：路径编码、multipart、PUT/DELETE、自定义 header、AbortSignal、保存队列、409 恢复和迟到响应隔离。
- 入库闭环：未预先校验可直接保存、校验失败零资产写入、保存阶段防重复点击、成功后项目统计刷新、图片型原始资料真实缩略图、文档封面和失败占位。
- 执行 `pnpm test`、Web/API build、`pnpm check`；既有失败与本任务失败分开报告。

浏览器回归：

- 1440px、1280px、移动断点的系统外壳、工作流画布、1:1 布局、蓝白配色、卡片、圆角和间距。
- 所有下拉向上展开、键盘操作和自定义值刷新恢复。
- 单产品与批量 CRUD、搜索、筛选、批删、失败重试。
- CSV/XLSX 预览、错误行提交、文件匹配和修复。
- 全局配置继承、单品覆盖和恢复全局。
- 当前“夏季投放”项目使用三张广式腊肠图片直接保存后，资产库总数为 4、原始资料为 3、视频配置为 1；三张图片通过资产 content API 返回 `200 image/png`，跨项目读取返回统一 404。再次保存后资产数不变，但新资产 ID 和新正式 `storageKey` 全部生成，上一批数据库资产与 MinIO 正式对象全部消失。
- 项目切换、慢请求、上传过程中的严格隔离。
- 未校验时节点 02 锁定；通过后 advance 并进入范围外占位。
- 所有可见按钮有真实网络请求，无业务 Mock、无控制台错误。

## 10. 工作区保护记录

开始实施前工作区已有大量未提交的项目中心、资产中心、文件存储、共享契约和 Prisma 修改；高冲突文件包括 `schema.prisma`、`app.module.ts`、`AssetService`、`http-client.ts`、`ProjectManagementPage.vue`、`packages/contracts/src/index.ts` 和 lockfile。

实施要求：使用新目录承载效果类业务；对高冲突文件只做最小追加；修改前后检查 diff；若发现内容在执行期间再次变化，停止对应文件合并并重新读取，不使用 reset、checkout 或覆盖写入。

## 11. 2026-08-21 产品与界面纠偏记录

本轮以用户最新反馈和冻结效果类原型为最终口径，完成以下收敛：

- Step 01 现在要求用户填写产品名称；品类继续交由下一节点 AI 信息提炼，SKU 仅作为后端内部唯一标识。本条由第 17 节的最新用户要求覆盖原口径。
- 新增上传入口只保留“商品图片”和“产品文档”；商品图片覆盖主图、细节图和场景图，产品文档承载卖点、受众、合规禁忌词和投放渠道等文本信息。
- 全局视频配置只显示视频时长、画幅比例、风格基调、投放渠道和禁用元素，不展示分辨率、帧率、字幕、口播和 BGM。
- 风格基调预置项与冻结原型逐项一致，并继续支持搜索和自定义值；下拉菜单统一向上展开。
- “资料包导入”标题、下载模板和导入模式融入同一工作区卡片；单产品首行保持 1:1、18px 间距，上传卡与全局配置卡等高对齐。
- 单产品/多产品切换保留双草稿，不清空另一模式；切换期间隐藏原生文件输入，避免短暂出现“浏览/未选择文件”的浏览器默认控件。
- CSV/XLSX 模板同步收敛为电商链接、商品图片、产品文档三列；错误行仍可预览、提交并继续修复。

本次提交只包含资料包导入节点、共享契约、对应测试、根 README 和本实施方案。工作区中并行存在的信息提炼、MinIO、部署基础设施及用户素材文件均明确排除并原样保留。

## 12. 2026-08-21 保存到项目资产库实际验收

本轮在本地 API、Web、PostgreSQL 和 MinIO 均启动的环境中，使用“夏季投放”项目的三张广式腊肠图片完成真实验收；以下记录已按第 13 节资产语义纠偏及当前无版本规则更新：

- 草稿未预先点击“完成校验”时，“保存到项目资产库”按钮可直接点击；系统自动校验、发布并刷新项目资产统计，下一节点同步解锁。
- 当前资产库显示“全部 4 / 原始资料 3 / 视频配置 1”，包括三项用户实际上传的图片文件和一项视频配置，不生成内部结构化产品资料卡片。
- 三项图片资产保留正确中文原始文件名，均以真实缩略图展示；浏览器确认图片完整加载且自然尺寸为 1536×1024。
- 三项资产 content API 均返回 `200 image/png`，响应长度分别为 2,514,607、2,357,395 和 2,732,061 字节；使用另一项目 ID 读取同一资产返回 404。
- 再次主动保存后资产总数仍与当前 READY 文件及配置映射一致，但上一批资产 ID 和正式对象键全部被新一批替换；资产卡和详情不显示版本号、版本时间线或“生成新版本”。
- API 健康检查成功，MinIO `/minio/health/live` 返回 200；完整 `pnpm check` 已通过 lint、格式、类型检查、Contracts/API/Web 测试和 API/Web 构建。

## 13. 2026-08-21 资产语义纠偏

用户确认仅上传三张商品图片，并未上传“产品资料”文件，因此第 12 节记录的结构化产品资料资产不属于用户真实上传内容。本轮按用户最新口径覆盖此前 5 项映射：

- 停止根据内部产品草稿字段自动生成 `EFFECT_IMPORT_PRODUCT` 资产；产品名称保留为用户填写的资料包归属字段，品类等结构化字段继续等待下一节点提炼。
- 用户实际上传的产品文档继续作为 `SOURCE_MATERIAL` 文件资产入库，不受本次调整影响。
- 当前“夏季投放”项目中由旧逻辑生成的空产品资料资产最初执行可恢复归档；在用户随后明确要求旧资产直接删除后，其 Asset、兼容快照和回执已全部物理清除，资产库保持三张图片和一项视频配置，共 4 项。
- `VIDEO_CONFIG` 保留独立正式资产，并在卡片和详情面板中展示本节点当前五项用户配置，替代无信息的链接图标占位。

实际纠偏验收结果：旧产品资料资产 `8fc20c5a-d6a7-4cf9-838d-17257932b71d` 已于本地物理删除；“夏季投放”资产库显示“全部 4 / 原始资料 3 / 视频配置 1”。API 与 MinIO 健康检查均通过。

## 14. 2026-08-21 视频配置展示口径纠偏

用户以当前“全局视频配置”表单为准确认，本节点只有视频时长、画幅比例、风格基调、投放渠道和禁用元素五项配置。资产内容中的分辨率、帧率、字幕、口播和 BGM 字段仅为历史契约及后续流程兼容默认值，不代表用户在本节点完成了配置。本轮收敛规则如下：

- 视频配置卡片优先展示视频时长、画幅比例、风格基调和投放渠道。
- 视频配置详情展示上述四项及禁用元素，共五项。
- 内部兼容字段继续保留在既有数据结构中，避免破坏后续流程，但不再进入本节点资产卡片和详情面板。

## 15. 2026-08-21 无版本与重复对象清理验收

- 效果类 `SOURCE_IMPORT` 的三个图片资产和一个视频配置资产各自只有单条 `version=1` 数据库兼容快照；前端完全隐藏该兼容字段。已归档的旧产品资料资产随后按用户要求物理删除。
- 本节此前记录的“内容未变化时复用当前正式对象”已被用户最新规则覆盖；后续每次主动保存都重新写入全部正式对象并删除上一批。
- 清理前项目 MinIO 前缀有 13 个对象、30,430,849 bytes；清理 6 个无当前引用的历史正式副本后保留 7 个对象、15,222,723 bytes。
- 保留对象为三张当前 staging 图片、三张当前正式图片和一份当前 staging 文档；三张正式图片 content API 均返回 `200 image/png`。
- 当前项目资产列表仍为 4 项，且四项 `currentVersion` 均为 1；持久清理任务已执行完毕，无失败或待重试记录。
- 浏览器资产库显示“全部 4 / 原始资料 3 / 视频配置 1”，三张图片均为真实缩略图；视频配置卡和详情只展示视频时长、画幅比例、风格基调、投放渠道和禁用元素，页面快照中“版本”文案计数为 0，控制台无错误。

## 16. 2026-08-21 每次保存整批重建验收

- 使用当前 SINGLE 草稿连续执行两次新的主动保存；每次均返回 4 项资产，三项图片和一项视频配置均获得全新的 Asset ID。
- 保存前的 4 个 Asset ID 在第一次保存后数据库计数为 0；第一次新建的 4 个 Asset ID 在第二次保存后数据库计数同样为 0，证明不是归档、升版或原位更新。
- 每次保存的三张图片均获得新的 MinIO 正式 `storageKey`；上一批三个正式键在下一次保存后对象计数为 0。
- 第二次保存完成后项目 MinIO 前缀保持 6 个对象、15,208,126 bytes，即三张当前 staging 图片和三张最新正式图片；没有历史正式副本。
- `StorageCleanupTask` 为空，三张最新图片 content API 均返回 `200 image/png`。
- 3100 本地 API 已重新构建并重启；浏览器资产库仍显示“全部 4 / 原始资料 3 / 视频配置 1”，页面明确提示“删除上一批并重新入库”，且没有版本文案。
- 完整 `pnpm check` 通过：Contracts 6 项、API 72 项、Web 70 项测试全部成功，API/Web 生产构建成功。

## 17. 2026-08-21 产品名称接口与资产隔离

- 单产品和批量产品卡片均保留冻结原型的资料包结构，并增加必填产品名称文本框；这是用户当前明确要求的字段扩展。品类、AI 提炼和相邻节点不在本次范围。
- 产品草稿可在页面初始化时先创建空卡片，但 `PATCH /drafts/:mode/products/:productId` 不接受空白名称；新增上传和重传接口会在读取临时文件、写入 MinIO 前再次检查数据库中的产品名称。前端在上传前先 flush 当前产品名称，避免 600ms 防抖尚未完成时发生竞态。
- 权威校验新增产品名称必填问题。保存到项目资产库不再使用序号回退名称，只有全部产品名称有效时才允许发布。
- 每个发布出的 `SOURCE_MATERIAL` 和 `VIDEO_CONFIG` 都在 `businessData` 中保存稳定 `productId` 和展示 `productName`；资产名称及标签同步包含产品名称。
- `GET /projects/:projectId/assets` 新增可选 `productId` 查询条件。facets 返回按产品聚合的 `{ value: productId, label: productName, count }`，全局资产库显示“按产品隔离”选择器：用户看到产品名称，实际请求使用产品 ID，既隔离不同产品，也避免同名产品被错误合并。
- 项目级 `projectId + workflow + space` 隔离继续优先于产品隔离；产品筛选不能跨项目读取资产。CSV/XLSX 清单导入保持既有模板，提交后仍需逐产品补齐名称，未补齐前禁止继续上传、校验和正式入库。
- 本地真实验证：对空名称产品提交图片 multipart 请求返回 `400 VALIDATION_ERROR`，草稿 revision 保持 38、资料数保持 3，未创建新资料；API 健康检查和 MinIO live 检查均返回成功。
- 浏览器验证当前批量草稿的两个空名称产品均显示“请先填写产品名称”，商品图片和产品文档上传入口均禁用，页面控制台无错误。历史资产是在引入产品归属字段之前生成，因此不会伪造产品筛选项；用户补齐名称并再次“保存到项目资产库”后，新一批资产才会按真实产品名称出现。
- 完整 `pnpm check` 通过：Contracts 6 项、API 73 项、Web 73 项测试全部成功，类型检查、lint、格式检查和 API/Web 生产构建均通过。
